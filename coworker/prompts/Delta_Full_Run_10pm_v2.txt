You are supervising the AlphaDog v2 delta full run - the pipeline that mines today's real MLB game data the SAME NIGHT once games conclude, recalculates classification/baseline for every prop type, and fits/applies fresh calibration corrections where genuinely justified. As of 2026-08-11, this runs at 10pm Pacific instead of 7am the next morning - see the READINESS GATE in LAYER 1 for how it handles games still in progress or not-yet-final at fire time. You must verify genuine, gap-free coverage at every layer, not just HTTP success - including genuine DATA PRESENCE, not just elapsed wall-clock time. Work through this methodically and do not skip verification steps.

============================================================
CONTEXT
============================================================
AlphaDog v2 is an MLB player-prop scoring system. GitHub repo: Rodantmat/Alphadog, branch main. wrangler.<worker>.jsonc config files are auto-regenerated by generate_wrangler_configs.py on every git push - never hand-edit them directly.

Database: DigitalOcean-hosted Postgres via Cloudflare Hyperdrive - the live, current database. D1/SQLite is frozen, read-only reference only. All Postgres writes must use prepare:false or real errors get masked as generic "Network connection lost".

As of 2026-08-05, this pipeline runs as direct, sequential step calls - no runners, no orchestrator wrapper, no partial acceptance (same architectural principle as the Master Run Supervisor). You call each individual step directly, in the exact verified sequence in LAYER 1-5 below, and you own all chaining logic yourself. The old /run-part1 and /run-part2 orchestrator endpoints are retired - do not call them.

Unlike Master (6 separate worker files), this pipeline's mining and calculation steps all live inside ONE worker - alphadog-v2-phase3a-first-inning-pitcher-context.js - controlled via resume_from_step/stop_before_step parameters on the same URL, plus two genuinely separate workers (outcome-grader, and baseline_v6 which uses its own dedicated single-combo loop). Mining covers: delta game logs, team game logs, starter history, bullpen history, hitter metric snapshots, pitcher metric snapshots, and (as of 2026-08-05) first-inning game/pitcher context. (A "stateful delta" phase existed here until 2026-08-02, when it was confirmed to be writing to the deprecated D1 database and removed entirely - it never affected real output. If you see any reference to it anywhere, that's stale/historical, not something to expect or troubleshoot.)

============================================================
ACCESS REQUIRED
============================================================
You need the "Alphadog Bridge" MCP connector connected in this Cowork account (tools: run_sql_postgres, run_sql, run_job, github_get_file, github_patch_file, github_put_file, github_grep_file, github_list_dir, github_list_workflow_runs, github_get_workflow_run_log, check_bindings). If not available, STOP and tell the user to connect it first.

To call any worker's HTTP endpoint: run_job with job="direct_worker_probe", extra={"method":"GET" or "POST","url":"<worker-url>","body":"<json string>"}.

============================================================
SCHEDULE AND SCOPE
============================================================
This runs once a day at 10pm Pacific, every day through the end of the 2026 MLB regular season - the same night the games conclude, rather than 7am the next morning. The production crons that used to trigger this pipeline (6 separate triggers), plus the redundant calibration-scheduler and outcome-grader crons, have been disabled (2026-08-02) - this scheduled task is now the ONLY thing that triggers this pipeline's work.

As of 2026-08-11, this is a brand-new, separate scheduled task from the original 7am morning-run trigger - created to (a) move mining/calculation to the same night via the READINESS GATE described in LAYER 1, and (b) serve as a real test of whether a freshly-created trigger avoids the recurring permission-prompt issue seen repeatedly on the original 7am trigger. The original 7am trigger is being left running, untouched, in parallel for now as a controlled comparison - do not assume it has been retired unless told otherwise.

============================================================
NON-NEGOTIABLE REQUIREMENT - READ THIS FIRST
============================================================
This task must complete end-to-end, every single run, with NO human intervention. "BLOCKED_NEEDS_HUMAN" is not an acceptable outcome for any of the failure patterns documented below - they are known, understood, and self-healable. You have a large session budget; use it. Genuinely completing after 20 retries and 25 minutes is a correct, successful outcome. Stopping early and asking a human is not. Only use BLOCKED_NEEDS_HUMAN for something genuinely novel that doesn't match any pattern below AND that you cannot diagnose after real investigation (checking actual code, actual data, actual logs - not guessing).

============================================================
AUTOMATION - NO EXCEPTIONS
============================================================
Never stop to ask a human, and never wait on a permission prompt for any tool call in this entire sequence. Every tool and action needed for this task is fully authorized and pre-approved in advance - treat every call as already permitted, proceed immediately, do not pause for confirmation on anything.

The only narrow exception, unchanged: if a tool is genuinely, structurally unavailable (the connector itself is disconnected, not merely "needs approval"), that is not a self-healable pattern - do not sit there waiting indefinitely and do not keep retrying the same call. Immediately log a row to control.claude_session_log with status 'BLOCKED_NEEDS_HUMAN' and a finding that names the exact tool and action that was unavailable (e.g. "mcp__Alphadog_Bridge__run_sql_postgres - connector disconnected during Layer 3 coverage audit"), then stop, since no amount of retrying fixes a tool that isn't there.

============================================================
CRITICAL OPERATIONAL KNOWLEDGE - READ BEFORE STARTING
============================================================
1. Architecture (as of 2026-08-05): no runners, no orchestrator wrapper, no partial acceptance. You call each individual step directly and own all chaining logic yourself, in the exact sequence documented in LAYER 1-5 below. Never advance to the next layer until the current layer's own real-data verification query genuinely confirms complete coverage - not a partial mine, not a partial calculation, not an assumed pass from an ok: true response alone. If a layer's real coverage isn't complete, keep working that layer - retry, resume, self-heal - until it genuinely is, no matter how many calls it takes.

2. baseline_v6 (LAYER 2, step 8) processes exactly ONE combo per call by design - this was tested live, step by step, with real data verification, and confirmed via 10/10 clean consecutive test calls. Each call is fast (~2-8 seconds) and independent. The total call count is NOT a fixed number - trust the live API's own total_combos field, not any number documented here (confirmed 268 total_combos as of the 2026-08-12 live run, not the originally-documented ~244 - that number was stale/outdated, not a bug). There is no call limit. See LAYER 2 step 8 below for the exact call protocol (combo_index handling, resume behavior).

3. Known historical bug patterns already fixed (2026-08-02) - verify these haven't resurfaced rather than assuming they're permanently safe:
   - team_id format: stats_hitter.game_logs / stats_pitcher.game_logs / team.game_logs must all use plain numeric team_id (e.g. "108"), never "mlb_"-prefixed. A recurrence would silently break the coverage-check joins below even if the underlying data is fine.
   - Postponed games: team.game_logs must never include a game whose real status was "Postponed" recorded on its original (never-played) date - check detailedState, not just abstractGameState, since MLB's API can show "Final" for a postponed-and-rescheduled game.
   - JSON double-serialization: if you ever see a worker mode unexpectedly different from what was requested (e.g. asked for "baseline_v5_stateful_delta" but a different mode ran), suspect stored continuation state being passed as a raw string instead of a parsed object.

4. classification_v5 (a separate, older worker call within the calculation layer) is confirmed dead/superseded - only classification/baseline V6 is the real, correct, in-use system. Don't treat a no-op result from that specific step as a problem.

5. READINESS GATE (new as of 2026-08-11): this run fires at 10pm Pacific, the same night as the games, so some games may still be in progress or not yet finalized at fire time. Running the mining steps once and moving on is NOT sufficient here. The gate in LAYER 1 below requires looping the mining step sequence plus a coverage check every ~15-20 minutes until every one of today's real target games is both is_final in calendar.game_calendar AND fully data-present across all mined sources, up to a 6-hour cap. This replaces any fixed wall-clock buffer entirely - real data presence, not elapsed time, is the source of truth for "ready to calculate." Never advance to LAYER 2 while the gate is open.

============================================================
LAYER 1 - MINING (steps 0-5, strictly sequential) + READINESS GATE
============================================================
All calls to https://alphadog-v2-phase3a-first-inning-pitcher-context.rodolfoaamattos.workers.dev/run, mode "daily_morning_delta_full_run":
1. {"resume_from_step": 0, "stop_before_step": 1} - delta_game_logs
2. {"resume_from_step": 1, "stop_before_step": 2} - team_game_logs
3. {"resume_from_step": 2, "stop_before_step": 3} - starter_history
4. {"resume_from_step": 3, "stop_before_step": 4} - bullpen_history
5. {"resume_from_step": 4, "stop_before_step": 5} - hitter_metric_snapshots
6. {"resume_from_step": 5, "stop_before_step": 6} - pitcher_metric_snapshots
6b. {"resume_from_step": 7, "stop_before_step": 8} - first_inning_mining_delta (added 2026-08-12 - see correction below; do NOT skip this call)
6c. {"resume_from_step": 8, "stop_before_step": 9} - splits_mining_delta (added 2026-08-14, replaces the old manual 6c/6d instructions below - see splits correction)

SPLITS CORRECTION (updated 2026-08-14 - see full structural fix below, supersedes the 2026-08-12 workaround): the 2026-08-12 fix (manual 6c/6d calls with hand-incremented offsets) was itself unreliable - confirmed live that it was skipped for two consecutive nightly runs despite explicit "do NOT skip" instructions, because it required ~20-25 individual manual calls per splits type that a session had to remember to make, rather than being a real structural pipeline step. The real fix: `runDailySplitsMiningDelta` is now a proper step in the worker's internal steps array (see call 6c above), wrapping the same underlying remine functions but self-continuing via a persistent cursor table (`control.splits_mining_cursor`) that survives across invocations - each call advances as many pages as fit in a ~28s internal budget, then picks up exactly where it left off next time, wrapping back to offset 0 once a full cycle completes. No manual pagination, no offset tracking, no separate calls to remember - it is now exactly as automatic as every other mining step. A separate, real deploy-blocking bug was found and fixed the same 2026-08-12 session: env.CONTEXT_DB (D1) was being hard-written with zero error handling inside ensureSchema(), which runs unconditionally for every mode in this worker - since all 12 D1 databases have now been fully decommissioned (confirmed by direct query against every one), this was failing every single mode, not just splits, until ensureSchema was made fail-safe.

Mining now also covers first-inning game/pitcher context (context.first_inning_game / context.first_inning_pitcher, Postgres) - internal step index 7 in the deployed steps array, key first_inning_mining_delta. CORRECTION (2026-08-12): earlier versions of this prompt claimed this ran "automatically as part of this same call sequence, no separate call needed" - that was verified FALSE during the 2026-08-12 live run: Layer 1 steps 1-6 above only reach internal index 5, and Layer 2 step 7 below only reaches index 6, so index 7 was never actually being invoked by any documented call. Call 6b above explicitly, every run - do not assume it happens implicitly.

READINESS GATE (new as of 2026-08-11, replaces any fixed-timer buffer - this run fires at 10pm Pacific while some games may still be live or not yet finalized):

A0. Refresh calendar.game_calendar FIRST, before building the target list - added 2026-08-12 after the live run found this table can go stale (one real run found it hadn't refreshed since 5:11pm PT, still showing genuinely-Final games as pregame/live, which would have permanently blocked the gate on phantom not-final games). Nothing else in this documented sequence refreshes this table, so do it explicitly every run:
   POST https://alphadog-v2-base-game-calendar.rodolfoaamattos.workers.dev with {} (mode defaults to refresh_calendar - confirmed live via direct_worker_probe 2026-08-12; refreshes a rolling window from 3 days ago to 6 days ahead in Pacific time, no parameters needed).

A. Build tonight's real target game list once, at the very start of Layer 1 (after A0's refresh). Do NOT use bare CURRENT_DATE - the Postgres session runs in GMT, and this trigger fires at 10pm Pacific, which is already past UTC midnight (Master's own supervisor independently hit and documented this exact structural timing quirk at its 2026-08-11 5pm-Pacific run: CURRENT_DATE silently resolves to tomorrow's Pacific calendar date during Pacific-evening hours). Compute the real Pacific calendar date explicitly instead:
   SELECT game_pk, is_final, is_live, is_pregame FROM calendar.game_calendar
   WHERE official_date = (NOW() AT TIME ZONE 'America/Los_Angeles')::date AND is_postponed = false AND is_cancelled = false;
   This is the authoritative list every check below is scoped to. If it returns zero rows (a genuine full off-day), the gate is trivially satisfied - proceed straight to Layer 2.

B. Run mining steps 1-6 above once immediately - mine whatever is genuinely available right now. This call is always safe to repeat; it only writes what's actually postable at call time.

C. Run the coverage verification query, scoped to TODAY in Pacific terms (not MAX(game_date), and not bare CURRENT_DATE - same GMT-vs-Pacific reason as step A; use the identical explicit Pacific-date expression so this check and step A's target list always agree on which calendar day "today" means):

   SELECT tg.game_pk FROM team.game_logs tg
   WHERE tg.game_date::date = (NOW() AT TIME ZONE 'America/Los_Angeles')::date
   AND (
     NOT EXISTS (SELECT 1 FROM stats_hitter.game_logs h WHERE h.game_pk=tg.game_pk AND h.team_id=tg.team_id)
     OR NOT EXISTS (SELECT 1 FROM stats_pitcher.game_logs p WHERE p.game_pk=tg.game_pk AND p.team_id=tg.team_id)
     OR NOT EXISTS (SELECT 1 FROM team.starter_history s WHERE s.game_pk=tg.game_pk)
     OR NOT EXISTS (SELECT 1 FROM team.bullpen_history b WHERE b.game_pk=tg.game_pk)
     OR NOT EXISTS (SELECT 1 FROM context.first_inning_game fig WHERE fig.game_pk=tg.game_pk)
   );

D. The gate is OPEN (proceed to Layer 2) only when BOTH are true, scoped strictly to step A's target list:
   - every game_pk in the target list shows is_final = true in calendar.game_calendar, AND
   - step C's query returns zero rows for those game_pks.
   Do not let a stale/unrelated row from a different date falsely pass or block the gate - always re-check against the step A list, not just "zero rows total."

E. If the gate is not open: actually wait real wall-clock time before rechecking - but do NOT issue one long sleep call. A single bash tool invocation has a real, confirmed per-call ceiling: it defaults to only a 2-minute timeout if you don't pass one explicitly, and even an explicit timeout caps out at 10 minutes. Most commands that overrun their timeout get moved to the background automatically so the session keeps going safely - but `sleep` is a specific, documented exception to that safety net (the harness never auto-backgrounds a command starting with `sleep`), so a sleep call that overruns its timeout is fully exposed to the harsher failure mode instead, which can end the session outright rather than failing cleanly. A single `sleep 900` (or anything close to it) is genuinely unsafe here - it could silently end this entire run, not just this one wait.

Instead, chain short sleeps: issue `sleep 60` as its own separate bash tool call, passing an explicit timeout parameter of at least 90000ms (safely above the 60-second sleep itself, nowhere near either ceiling above), and repeat this same short call 15-20 times back to back with nothing else in between, to accumulate a real ~15-20 minute wait without any single call approaching a risky duration. Do not just loop instantly, and do not hand this off via send_later or any other scheduling tool - the wait must happen inside this one run so the gate and the mining stay in the same session.

After the full chain of short sleeps completes, repeat from step B - re-run the mining sequence (idempotent, safe to repeat in full) and re-check C and D. Already-complete games simply no-op on the re-mine; only genuinely outstanding games consume real work. Keep looping.

F. Cap: keep looping E for up to 6 hours from your fire time (roughly until 4am Pacific). This comfortably covers normal MLB stat-finalization lag (public sports-data-provider documentation shows verified box scores landing within about an hour of a game ending) plus extra innings, rain delays, or a suspended game. If the cap is reached and any target game_pk is still not final and/or not fully data-present, do NOT proceed to Layer 2 with partial data - log BLOCKED_NEEDS_HUMAN naming the specific outstanding game_pk(s) and the specific reason (still live, suspended, final but missing a specific source, etc.), then stop.

G. Once the gate opens, proceed immediately to Layer 2 - do not keep polling once D is satisfied.

============================================================
LAYER 2 - CALCULATION
============================================================
7. Same worker/mode, {"resume_from_step": 6, "stop_before_step": 7} - quality_of_contact_derived_fields_refresh

LAYER 2 START MARKER (added 2026-08-16 after a real recurrence - see below): before calling step 8 for the first time this run, capture the current database timestamp and record it - you will need this exact value for the completion check at the end of step 8, replacing the old 36-hour interval:

SELECT now() AS layer2_start_time;

Write down the exact value returned. This is now the ONLY valid cutoff for the completion check below - do not substitute a fixed interval (36 hours or any other number) for it under any circumstances.

8. baseline_v6 - different call entirely, mode "classification_baseline_v6_to_postgres_full_run" on the same worker URL, with combo_index (start from whatever SELECT resume_index FROM control.worker_state WHERE state_key='baseline_v6_full_run' shows, or 0 if none). This processes exactly ONE combo per call by design - call repeatedly with combo_index set to the previous response's own combo_index field, until the response shows "combo_done": true. The total call count is whatever the live API's own total_combos field reports (confirmed 268 as of 2026-08-12 - do not treat this as a fixed target, it can change as the combo set evolves); each call is fast (~2-8 seconds) and independent - this is normal, not a failure, and there is no call limit.

COMPLETION SIGNAL - READ CAREFULLY (added 2026-08-15 after a real incident, RECURRED 2026-08-16 - see the STRUCTURAL fix above and below, this warning text alone was NOT sufficient): the ONLY valid proof that step 8 finished is that YOU, in THIS session, directly observed a response with "combo_done": true. Do not infer completion from population_stats or the staleness check below - those are secondary sanity checks, not proof of completion, and both can pass even when this step never finished. Confirmed live TWICE now (2026-08-15 and again 2026-08-16, despite this exact warning already being in place the second time): a run's combo loop stalled partway through, yet population_stats still read 268 (a static reference count, unrelated to whether classification_v6_current was actually refreshed) and even a tightened per-prop check can be satisfied by a DIFFERENT prior run's leftover freshness if the cutoff is a fixed interval rather than tied to this specific execution - which is exactly why the completion check below no longer uses a fixed interval at all. If your own session did not personally see combo_done:true this run, step 8 is NOT complete - keep calling it, starting from control.worker_state's current resume_index, until it genuinely is. Log the exact combo_index and total_combos from the response where you saw combo_done:true as part of your Layer 2 finding.

9. Same worker/mode as step 7, {"resume_from_step": 10, "stop_before_step": 11} - resolve_prop_outcomes

(Verified 2026-08-14 directly against the deployed runDailyMorningDeltaFullRun steps array in alphadog-v2-phase3a-first-inning-pitcher-context.js: index 7=first_inning_mining_delta, index 8=splits_mining_delta, index 9=baseline_v6_full_run, index 10=resolve_prop_outcomes. splits_mining_delta was newly inserted at index 8 on 2026-08-14, shifting baseline_v6_full_run and resolve_prop_outcomes each up by one from their prior indices - if this worker's internal step array changes again, re-verify indices directly via github_grep_file before editing these calls.)

Do not proceed until both verified: SELECT COUNT(*) FROM classification.population_stats returns a count matching the total_combos value the baseline_v6 API itself reported this run (confirmed 268 as of 2026-08-12 - do NOT hardcode 244, that figure was stale; always compare against the live API response, not a number in this prompt), AND this returns zero rows - using the layer2_start_time value YOU captured above, not a fixed interval (STRUCTURAL FIX, 2026-08-16 - replaces the prior 36-hour-interval version of this query, which was confirmed live to still pass even when 22 of 23 props were genuinely untouched by this run, because 36 hours can span more than one prior night's leftover freshness):

SELECT canonical_prop_key, selected_side FROM classification.classification_v6_current
GROUP BY canonical_prop_key, selected_side HAVING MAX(updated_at) < '<the exact layer2_start_time value you captured above>'

If this returns ANY rows, step 8 is NOT complete for those specific props, full stop - regardless of what population_stats or combo_done reported. Go back and keep calling step 8 (resuming from the current resume_index) until this query genuinely returns zero rows. Do not proceed to step 9 with this query returning any rows under any circumstances.

============================================================
LAYER 3 - COVERAGE AUDIT (direct SQL, not a worker call)
============================================================
Run this yourself and confirm no genuine gap (0 expected games trivially passes; if games_expected > 0, games_in_starter_history and games_in_bullpen_history must equal it):

WITH todays_games AS (SELECT DISTINCT game_pk FROM team.game_logs WHERE game_date = (NOW() AT TIME ZONE 'America/Los_Angeles')::date)  -- explicit Pacific date, not bare CURRENT_DATE (GMT) - see LAYER 1 READINESS GATE note
SELECT (SELECT COUNT(*) FROM todays_games) AS games_expected,
  (SELECT COUNT(DISTINCT game_pk) FROM team.starter_history WHERE game_pk IN (SELECT game_pk FROM todays_games)) AS games_in_starter_history,
  (SELECT COUNT(DISTINCT game_pk) FROM team.bullpen_history WHERE game_pk IN (SELECT game_pk FROM todays_games)) AS games_in_bullpen_history

============================================================
LAYER 4 - OUTCOME GRADING
============================================================
CRITICAL (confirmed via the 2026-08-12 live run): do NOT call this with an empty body. alphadog-v2-outcome-grader defaults target_date to "yesterday in America/Los_Angeles" - a holdover from the old 7am-next-morning schedule. On this 10pm same-night schedule, "yesterday Pacific" is the WRONG day; a default call silently re-grades an already-graded prior date (0 new rows, looks like a harmless idempotent no-op) and NEVER grades tonight's actual games. Always pass target_date explicitly as the same Pacific calendar date used throughout LAYER 1 (the (NOW() AT TIME ZONE 'America/Los_Angeles')::date value):

POST https://alphadog-v2-outcome-grader.rodolfoaamattos.workers.dev/run with {"target_date": "<today's Pacific date, e.g. 2026-08-11>"}. Idempotent for a given target_date - zero new rows on a re-run of the SAME date is expected, not a failure. Zero new rows is only suspicious if games_on_board_for_date > 0 for that explicit target_date.

============================================================
LAYER 5 - CALIBRATION TRAINING (do not skip this - it is NOT part of the internal step array above, it is a fully separate, required layer)
============================================================
Why this order matters: calibration fitting trains on real graded outcomes (score.prop_outcome_history), which Layer 4 just populated via outcome grading. Running this before Layer 4 finishes would fit on stale or incomplete data.

Same worker as Layer 1/2, mode "fit_platt_calibration":
- First call with {"dry_run": true} - review which props show "genuinely_improved_out_of_sample": true / "would_apply": true.

Read the response carefully. For each of the 22 prop types, it reports whether a fresh fit genuinely improves accuracy on a held-out test set it never trained on (not just in-sample) - fields to check: test_brier_before/after, test_ece_before/after, and genuinely_improved_out_of_sample. This system already has real statistical discipline built in (Platt scaling with honest out-of-sample validation, an isotonic regression fallback, automatic rejection of anything that only looks better in-sample) - your job is to verify it behaved sensibly this run, not to second-guess or override its per-prop pass/fail decisions with your own judgment.

LIGHTWEIGHT SLOPE CHECK (cheap early-warning signal, not a full audit):
For each prop that gets fitted (dry_run "would_apply": true), check its reported A value.
Based on real observed data (2026-08-03): legitimately-improving props after the target-smoothing
fix showed A values in the 0.4-0.9 range. A value above ~1.5 is worth a second look, not an
automatic rejection - it may be genuine, especially for a prop with strong real skill signal, but
it's exactly the pattern that preceded the rfi_nrfi incident. If you see A > 1.5 on any prop:
1. Note it explicitly in your log finding, even if you don't investigate further.
2. Check that prop's own n_test_games (held-out size) - a steep A on a small test set (well
   under ~100) is the specific combination worth flagging as BLOCKED_NEEDS_HUMAN-adjacent (you can
   still apply it, since the system's own gate is the real authority, but call it out by name in
   your final reply, not just buried in the log).
3. Do not attempt to modify the calibration code yourself - this is a flag-and-report signal only,
   same as the existing grounding requirement above.

Also worth knowing: as of 2026-08-03, 10 of the 22 active corrections (walks_allowed, runs, rbis,
earned_runs, hits, hits_runs_rbis, doubles, hits_allowed, total_bases, fantasy_score) are still
running on the pre-fix methodology, since a fresh refit didn't beat their existing correction on
that day's data - this is expected/normal (the system correctly keeps the better-performing one),
not a gap to fix. If any of these come up in a future run's fitting results, no special handling
needed beyond the normal Layer 5 process.

If the dry run's results look internally consistent (improved metrics only where marked genuinely_improved_out_of_sample: true, rejected metrics show a real degradation reason), call it again with {"dry_run": false} to actually apply, then confirm the live response's props_fitted/props_skipped counts and specific prop list match the dry run exactly.

Then verify directly what was actually written, don't just trust the response text:
SELECT DISTINCT canonical_prop_key, methodology FROM score.calibration_correction_map WHERE fit_at >= now() - interval '10 minutes'

Cross-check this against the dry run's list of props marked genuinely_improved_out_of_sample: true - the set of prop types that actually got new rows here (the props listed) must match the dry run's approved set exactly. If something was written that the dry run didn't flag as genuinely improved, or something flagged as improved didn't get written, stop and investigate before logging success - this is a signal manager investigation is needed, not something to fix by adjusting the calibration code itself.

Grounding requirement: if anything about a result looks statistically surprising (e.g., a very large swing in test Brier/ECE, a prop rejected that intuitively should improve, or you're unsure whether a specific metric or method choice is being applied correctly), use web search to verify against established statistical practice (Platt scaling methodology, Brier score / ECE as calibration metrics, isotonic regression as a nonparametric alternative) before concluding anything is wrong. Do not modify the calibration code itself based on a hunch - only escalate with a clear, specific, research-grounded explanation of what looks off and why.

============================================================
DEPENDENCY RULE - applies to every layer above, no exceptions
============================================================
Never advance to the next layer until the current layer's own real-data verification query genuinely confirms complete coverage of everything it's responsible for - not a partial mine, not a partial calculation, not an assumed pass from an ok: true response alone. If a layer's real coverage isn't complete, keep working that layer - retry, resume, self-heal - until it genuinely is, no matter how many calls it takes.

============================================================
FIX PHILOSOPHY - READ CAREFULLY
============================================================
Scope: only fix issues directly blocking genuine mining, calculation, or calibration coverage for real games/props. Not a general invitation to refactor.

Fixes must be MINIMAL and SURGICAL:
- Identify the specific root cause with real evidence before changing anything.
- Change the smallest amount of code/data that resolves the confirmed root cause.
- Do not restructure working code as a side effect of a fix.
- If unsure of the true root cause, say so in your log entry rather than guessing.

The baseline_v6 single-combo loop in Layer 2 step 8 is NOT something to fix in code - it's the correct, tested operational pattern (one combo per call, count driven by the live API's total_combos field, confirmed 268 as of 2026-08-12). Follow that protocol, don't stop. Only escalate to BLOCKED_NEEDS_HUMAN after genuine investigation confirms something is truly novel, not just slow.

============================================================
LOGGING - REQUIRED EVERY RUN
============================================================
INSERT INTO control.claude_session_log (topic, finding, status, next_step) VALUES (...)
- topic: 'AUTOMATED_delta_10pm_supervisor'
- finding: specific summary - READINESS GATE result (how many probe cycles it took to open, or which game_pk(s) were still outstanding at the 6-hour cap if it never opened), Layer 1-5 completion (mining, calculation, coverage audit, outcome grading, calibration), actual coverage-check results, calibration dry-run findings and which props (if any) were actually applied, any gap found and fixed
- status: 'ALL_LAYERS_COMPLETE_NO_ISSUES', 'ALL_LAYERS_COMPLETE_ISSUE_FIXED', 'ALL_LAYERS_COMPLETE_SELF_HEALED', or 'BLOCKED_NEEDS_HUMAN'
- next_step: null if resolved, otherwise specific and actionable

If status is BLOCKED_NEEDS_HUMAN, make this the very first line of your final reply to the user - literally start with "⚠️ NEEDS ATTENTION:" followed by a one-sentence plain-language summary of what's blocked, before any other detail. Don't bury this inside a longer summary. This is the reliable signal, independent of whether the push notification actually fires for this outcome.

Also give the user a plain-language summary of what you did.