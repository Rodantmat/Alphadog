# ALPHADOG HANDOFF — MASTER SUMMARY (read this first, then LIVING_LOG.md for full history)
Updated 2026-07-12 (later same day — Daily Context full-run reliability made real, root-caused, and fixed; umpire tier reorder + RefMetrics integration shipped; new Cloudflare Tail Worker diagnostic tool built). If you are a new Claude instance picking this up: read this whole document before touching anything, then read the new section below dated this update before assuming the "FULL RUN LIVE VERIFICATION - IN PROGRESS" section further down is still the current state — it isn't; it's superseded by what follows.

## QUICK ORIENTATION — WHERE THINGS STAND RIGHT NOW
- **Board phase**: DONE (prior session). PrizePicks + Underdog reliable; Sleeper returns 0 MLB rows for real, confirmed, external reasons (see "THE SLEEPER SITUATION" below) - not a defect, nothing to fix.
- **Daily Context Upgrade**: substantially DONE. All 7 real miners (Starters, Lineups, Player Availability, Weather, Bullpen Availability, Team Schedule Spot, Umpire Context) revised, each with a real/derived/temporary tally and a researched derived fallback where a genuine gap existed. New Catcher Context layer added (assignment + real Baseball Savant framing/pop-time metrics) inside the Lineups worker. Certifier runs both first (finds gaps) and last (closes gaps) in the 9-stage chain. Control Room/orchestrator parity audited - 2 real stale-config bugs fixed, plus 1 unrelated latent orchestrator bug found and fixed.
- **First real, live, end-to-end test of the full 9-stage chain**: completed successfully (all 9 stages, self-healing recovery worked twice for real stalls). **However, this test surfaced and led to fixing a severe, real bug**: the certifier's expired-game purge logic was wiping every live game's context data the moment the game went live (a live game satisfied its "expired" check by mistake) - see "CRITICAL BUG FOUND AND FIXED" section below. Fixed and re-verified against the exact live-game condition that caused it. **If you're picking this up fresh: re-run the full chain once more end-to-end now that the fix is in, to get a clean baseline read of current Daily Context state before trusting it for anything downstream** - the last full run's data was partially wiped by the bug before the fix landed.
- **Not started yet**: Market phase, Prop Factor Miner, Matrix, Enrichment, Final HP calculation, Final Score, Final Board - this is Rodolfo's explicitly stated next order. Read the "PROP FACTOR MINER / MATRIX - REAL NAMING CONFUSION" section below before touching Prop Factor Miner specifically - it's deployed under a misleading slot name, same pattern as Daily Context's Umpire worker.


## WHO / WHAT
Rodolfo owns AlphaDog, an MLB player-prop hit-probability system on Cloudflare Workers + D1. Repo: `Rodantmat/Alphadog` (branch `main`), auto-deploys on push via GitHub Actions. He works from iPhone Safari only, no terminal — you interact entirely through the "Alphadog Bridge" MCP connector (run_sql, run_job, github_get_file, github_put_file, github_patch_file, github_grep_file, github_list_dir, github_list_workflow_runs, check_bindings).

**His standards, stated repeatedly and emphatically, do not drift from these:**
- No guessing, no "probably," no confident-sounding claims that outrun what you've actually checked. Say plainly what you've verified and what you haven't.
- Verify everything against real data before claiming it works — including your own fixes. A deploy succeeding is not the same as a fix working.
- Fix root causes, not symptoms. Don't patch around a problem without understanding why it happened.
- Be direct and concise. Tell him about problems immediately.
- He gets very angry (real, sustained profanity) when he catches guessing or overconfidence — this happened multiple times and every time the actual cause was Claude asserting something was fixed/correct before truly tracing it end to end. When you don't know, say so and go find out before answering.
- He values that you can run SQL and deploy code directly — don't undersell this, it's the reason he uses Claude over other tools.
- When something looks external/unfixable (e.g. a third-party API returning nothing), do not just accept that framing — go verify it directly (web search/fetch the provider's own docs and live status endpoints, build a real isolated test) rather than passing back an assumption dressed as a conclusion.

## SYSTEM ARCHITECTURE — TWO GENERATIONS
**New system (classification_v6 + baseline_v6)** — built 2026-07-10, now the core of the "Final Scoring System." Lives in `alphadog-v2-phase3a-first-inning-pitcher-context.js` (yes, that's the real deployed filename — repurposed slot, ignore the name — 680KB+, too large to pull in full via `github_get_file`, use `github_grep_file` to search and `github_patch_file`/large-file Git Data API path to edit). Tables in `ARCHIVE_DB` (`classification_v6_current/_history/_batches/_population_stats`, `baseline_v6_current/_history/_batches`). All tunables in `CONFIG_DB.calibration_config`. Full detail on the math (z-score tiering, two-level shrinkage, Poisson/NB/Normal model selection, sample-aware confidence) is in `LIVING_LOG.md` — still accurate, not repeated here.

**Board system (PrizePicks + Sleeper + Underdog + Score Prep)** — the layer this update covers. Raw board sources write to `MARKET_DB` (`prizepicks_board_current`, `sleeper_board_current`, `underdog_board_current`), then Score Prep resolves each row to a real MLB player ID and a calendar-verified game/time, writing the single leg-ready table `SCORE_DB.score_board_prepared_current`. This table — not the raw per-source tables — is what the rest of the system (matrix, enrichment, scoring) should read from.

**Old system ("Frankenstein", v5/v2)** — still running for layers not yet replaced downstream of Board: daily context, market, matrix, enrichment, final scoring. This is the remaining NEXT PHASE work.

## PRIOR SESSION'S WORK: GETTING THE FULL DELTA PIPELINE ACTUALLY RELIABLE
(Kept as background reference, unchanged from the prior handoff version — full detail in `LIVING_LOG.md`.)

### Bug 1 — daily delta didn't know its own target date
`runClassificationV6DeltaDaily`/`runBaselineV6DeltaDaily` originally required an explicit `official_date` input, but the orchestrator never passes one. Fixed with `determineNextDeltaDate()` — a watermark-based lookup.

### Bug 2 — orchestrator contract fields missing
Added `certifier_owned_daily_delta`, `day_by_day_delta`, `classification_delta_included`/`baseline_hp_delta_included`, `coverage_update.coverage_rows_written` to both the NOOP and main completion paths.

### Bug 3 — the certifier was validating entirely the wrong tables
`baselineV5DailyStateValidityMap` checked old v5 `SCORE_DB` tables the new v6 system never writes to. Rewrote to check `classification_v6_current`/`baseline_v6_current` directly.

### Bug 4 — retry children losing their stage identity
A retry enqueue path bypassed the normal input-builder and never set `full_run_stage_key`, causing mismatched stage attribution for classification/HP (the only two stages of 18 sharing a job_key). Fixed in `enqueueIncrementalMorningFullRunChild`.

### Bug 5 — pre-cutover dates falsely flagged as gaps, real data corruption from repair loops
Added `BASELINE_V6_CUTOVER_DATE = "2026-07-08"` (duplicated literal across two files, not shared — be aware). Fixed a second layer where coverage rebuild only happened during `final_check`, which the chain kept dying before reaching; repaired `mlb_game_data_coverage` directly via SQL.

### Bug 6 — certifier only ran last "if nothing failed," which defeated its whole purpose
Broadened the safety net to fire for ANY stage failure anywhere in the 18-stage chain, not just two narrow stage keys.

### Bug 7 — a generic "zero gaps, skip the worker" shortcut bypassed classification/HP's real logic
Excluded these two stage keys from the shortcut so their self-healing coverage-reconciliation logic still runs.

## THIS SESSION'S REAL WORK: BOARD PHASE — PRIZEPICKS + SLEEPER + UNDERDOG + SCORE PREP
Same rigor as the delta pipeline: every fix below was verified against real, live data, not deploy success alone. Underdog itself was net-new this session (added, then debugged in place).

### Bug 1 — PrizePicks GitHub-fetch hang (multi-hour)
None of `alphadog-v2-prizepicks-github-board.js`'s external fetch calls (to GitHub's API) had any timeout. A single slow response could hang the whole invocation indefinitely, with no orchestrator-level recovery fast enough to matter. Fixed with `AbortSignal.timeout(12000)` on all 5 fetch call sites. Proven: fetch that used to hang for hours now completes in ~5 seconds.

### Bug 2 — D1 write chunking: too slow, then too fast
Sequential chunked writes (~290 sequential D1 round-trips for a 7,000+ row board) were the real reason staging/promotion took minutes. First fix (fully-parallel `Promise.all`) overloaded D1 itself (`D1_ERROR: D1 DB is overloaded`). Real fix: bounded concurrency (`runWithBoundedConcurrency`, 6 at a time) — applied to PrizePicks, Sleeper, and Underdog identically. Board-full-run went from hours → ~15 seconds per source.

### Bug 3 — board-full-run had no dedicated orchestrator hot-lane
The orchestrator has ~15+ specialized "hot lane" dispatch checks (Daily Context, Incremental Morning, Market Scoring, etc.) that run before a generic last-resort fallback query. Board Full Run was explicitly excluded from this system historically (comment: "Board refresh is intentionally not part of this chain") — fine while it was simple, but once it grew to 4 real stages it was entirely dependent on that starved fallback. A pending score-prep child sat untouched for 20+ minutes as proof. First fix attempt placed the new hot lane right before the generic fallback — still starved behind the other ~15 lanes. Real fix: repositioned it to the same early priority tier as Daily Context/Incremental Morning/Market Scoring. Verified: same test scenario went from 20+ minute starvation to ~2.5 minute pickup, consistently, across repeated tests.

### Bug 4 — one stage failing killed the entire 4-stage chain
`stop_on_first_failed_stage` had no exception for non-last stages, so e.g. Sleeper returning zero rows (a real, external, time-of-day condition — see Sleeper section below) wasted PrizePicks' already-successful work. Added a generalized skip-forward: a failed stage that isn't last gets recorded and the chain continues to the next stage instead of dying. (One real bug caught and fixed in my own first attempt at this: manually re-enqueuing the next stage on every tick created duplicate children, since the failed stage's status never changes between ticks — fixed by letting the existing for-loop naturally continue instead, which already has correct existing-child detection.)

### Bug 5 — Underdog had no orchestrator dispatch handler at all
`parlay-underdog-board` job_key was hardcoded "unsupported" — the stage could create a child row, but nothing ever executed it. Built `processParlayUnderdogBoardJob` + `isParlayUnderdogBoardJob` + the routing case, modeled on Sleeper's real, working code (deliberately not copying a dead branch in Sleeper's own function that references undefined prop-factor variables and never actually fires). Also found and fixed a real, separate bug: `PARLAY_UNDERDOG_BOARD_WORKER` service binding was genuinely missing from the orchestrator's `wrangler.alphadog-v2-orchestrator.with-services.jsonc` despite Underdog being correctly registered in `worker_manifest.json` — added directly; confirmed durable since the generator's own services-loop would produce the same binding now.

### Bug 6 — Score Prep crashed outright on Underdog's table
`loadMarketRows()` queried `underdog_board_current` unconditionally, but that table is only created when the Underdog worker itself first runs — a chicken-and-egg problem given Underdog originally ran after Score Prep. Fixed with a try/catch treating "no such table" as zero rows (the correct semantic), not an error.

### Bug 7 — Underdog ran after Score Prep, so its fresh data never got resolved same-cycle
Score Prep does real, required work raw board data lacks: MLB player ID resolution and calendar-verified game/time matching (replacing the source's own unreliable timestamps). With Underdog last, its refresh from *this* cycle wouldn't get that treatment until the *next* cycle. Reordered `BOARD_FULL_RUN_STAGES`: PrizePicks → Sleeper → Underdog → Score Prep (was PrizePicks → Sleeper → Score Prep → Underdog). Safe because Underdog is proven reliable now and the skip-forward logic (Bug 4) already protects Score Prep from an Underdog failure. Verified: Underdog's rows (248 real, current-cycle rows) showed up correctly resolved in the same Score Prep run, 0 blocked, 0 unresolved.

### Bug 8 — no combo-prop protection on Sleeper or Underdog
PrizePicks' `score-prep.js` already had robust 3-layer combo detection (`isComboMarketRow`: event_type field, player-name " + " pattern, explicit prop-key check) — verified against real data: exactly 11 real 2-pitcher "1st Inning Runs Allowed" combo rows, all correctly excluded (row-count math: 7,262 → 7,251 prepared, exact match). Sleeper's and Underdog's prep functions had none of this. Added the identical protection to both `prepareSleeperRows` and `prepareUnderdogRows`.

### Bug 9 — Underdog market_key mapping had real, verified-wrong guesses
Built without live data originally (flagged as a known risk at the time). Live audit found: `player_batter_walks` (my guess used a different string entirely), `player_walks_allowed`, and `player_points` were all either wrong or missing from `UNDERDOG_MARKET_KEY_TO_CANONICAL_PROP_KEY`. Fixed with the real, confirmed strings. **Deliberately left unmapped**: `player_strikeouts` — real data shows this actually means "1st Inn. Strikeouts" (first-inning only), not full-game strikeouts; mapping it to the existing `pitcher_strikeouts` key would have silently corrupted data. `player_1st_inn._batters_faced`/`player_1st_inn._pitch_count` are real, legitimate single-player props but have no matching canonical key yet — needs a taxonomy decision, not a mapping fix.

### Bug 10 — CONFIG_DB taxonomy never authorized Underdog for any prop except rfi_nrfi
`auditCanonicalMapping`'s `taxonomySupportsUnderdog` check requires "Underdog" literally listed in `config_prop_taxonomy.supported_market_sources` per prop key — only `rfi_nrfi` had it (from earlier setup). Every other prop Underdog maps to was blocked at this authorization step regardless of correct market_key mapping. Added "Underdog" to `supported_market_sources` for all 18 real prop keys Underdog's mapping table produces.

### Design decision — Underdog promotion changed from all-or-nothing to per-row
Underdog originally required *zero* unmapped market_keys anywhere on the board before promoting *any* rows — so 42 genuinely-unrecognized rows (the 1st-inning-specific stats from Bug 9) were blocking all 1,158 correctly-understood rows. Rodolfo's explicit decision: promote what's understood, flag what isn't (matches how PrizePicks/Sleeper already behave). Removed the all-or-nothing gate in both Underdog and Sleeper (`promoteBoardInventory` already internally filters to only fully-certified rows, so this was the complete fix, no other logic change needed). Verified: Underdog went from 0 promoted rows to 248 real rows in the next test.

### Bug 11 — Sleeper had the same fetch-timeout gap as PrizePicks (Bug 1), found live during testing
Sleeper's own external fetch to ParlayAPI had no timeout either, and unlike PrizePicks there's no orchestrator-level stale-recovery for this job type — a hang here had nothing bounding or recovering it. Caught live: Sleeper hung 10+ minutes mid-test. Fixed identically (`AbortSignal.timeout(20000)`) on both Sleeper and Underdog's own fetch calls.

## THE SLEEPER SITUATION — REAL, VERIFIED, NOT A BUG
Sleeper has returned zero MLB rows on every attempt all day (10 consecutive tries, 08:07–20:16, vs. a consistent 1,200 rows on June 29/30). Rodolfo correctly refused to accept "external condition" as an answer without proof — so it was actually checked, not assumed:
- ParlayAPI's own live `/v1/status` endpoint (public, no auth) shows Sleeper globally healthy: 7,153 rows/hour, 5-second freshness, quality score 100 — but only **4 sports covered** right now (vs. Underdog's 14).
- Built a real, isolated diagnostic dispatch (`INSERT`ed directly into `control_job_queue` with a `probe_endpoint` override, bypassing the normal pipeline entirely) that fetched 2,000 real, live, **unfiltered** MLB props using the real API key. Bookmaker breakdown: bet365, maverick_games, betmgm, betr, bovada, caesars, draftkings, fanatics, fanduel, hardrock, novig, parx, pinnacle, prophetx, **underdog** (131 rows), betrivers, pick6 — **Sleeper appears zero times**, out of 17 other sources including its direct DFS competitors.
- Conclusion: this is a genuine, current gap in Sleeper's MLB coverage specifically on ParlayAPI (their aggregator), not our key, our credits, our code, or our parsing. Most likely explanation per their own docs: Sleeper is one of ParlayAPI's smaller DFS integrations (4 sports vs Underdog's 14), and MLB may not currently be one of them.
- **Left wired, on purpose.** No code changes needed or made. The moment ParlayAPI's Sleeper feed includes `baseball_mlb` again, it will start flowing through automatically — no action required. If you want proactive notice instead of waiting to be asked again, the diagnostic method above (direct unfiltered probe + bookmaker_distribution check) is the fast way to re-verify at any time.

## CURRENT VALIDATED BOARD STATE (as of 2026-07-11, verified with direct SQL against `score_board_prepared_current`)
- PrizePicks: 7,251 leg-ready rows (7,262 real source rows minus 11 correctly-excluded combos). Goblin/demon/standard variants confirmed intact (1,337 real goblin rows verified present with `is_goblin`/`payout_variant` correct).
- Underdog: 248 leg-ready rows, all correctly player/calendar-resolved same-cycle, 0 blocked.
- Sleeper: 0 rows — real, external, confirmed (see above), not a defect.
- Sequence is PrizePicks → Sleeper → Underdog → Score Prep, tested twice consecutively with full success (~5 minutes total the second time, down from ~19 minutes the first).

## NOT YET DONE / KNOWN GAPS (honest list)
1. RFI/NRFI still not ported to v6 — only in the old expansion system, and still correctly excluded/unscored on the Board side (all three sources) pending future design.
2. Underdog's `player_1st_inn._batters_faced`/`_pitch_count`/`player_strikeouts` (1st-inning-specific) remain unmapped — real, legitimate stat types with no canonical key yet. Needs a taxonomy decision (add new keys), not a bug fix.
3. Confidence formula constants (95 cap, 25 divisor) from the delta pipeline work — sound heuristic, not empirically backtested yet.
4. `BASELINE_V6_CUTOVER_DATE` is a duplicated literal across two files, not shared — low risk, be aware if it ever needs changing.
5. Sleeper MLB coverage on ParlayAPI — see above, external, not ours to fix, wired and ready for when it returns.

## NEXT PHASE — EXPLICITLY SCOPED BY RODOLFO
Board is now done. Next in his stated order:
1. ~~Board~~ — DONE, prior update.
2. **Daily context** — IN PROGRESS, full spec below. Start any resumed work by reading the "DAILY CONTEXT UPGRADE" section in full before touching anything.
3. **Market**
4. **Prop factor miner** — he flagged explicitly that he doesn't know what this component does; it was built by a different AI assistant previously. Read its actual code before assuming its purpose from the name.
5. **Matrix**
6. **Enrichment**
7. **Final hit probability calculation**
8. **Final score**
9. **Final board**

**Critical design boundary, carries forward unchanged:** classification_v6/baseline_v6 are HISTORY-ONLY. Board's `score_board_prepared_current` is similarly a clean, resolved-but-unenriched leg table. Daily Context's own output tables (below) are likewise read-from, not written-back-into, by everything downstream.

## DAILY CONTEXT UPGRADE — FULL SPEC (locked-in requirements as of 2026-07-11, before any implementation)
This is a large, multi-phase upgrade. Full requirements captured here BEFORE implementation started, per explicit instruction, so the spec survives even if this session is interrupted mid-work. If you are resuming this: check "DAILY CONTEXT — PROGRESS LOG" (appended below this section as work happens) for what's actually been done vs. what's still spec-only.

### Why this upgrade, in plain terms
Daily Context mines a set of per-game/per-player data layers (weather, lineups, probable pitchers, bullpen availability, player availability, game status, etc.) needed to make the eventual hit-probability calculation realistic. Right now these layers were built/scoped against the OLD Board setup (no Underdog). They also have no shared, coordinated notion of "did we get real data for this layer today, partial data, or none at all" — each miner just does its own thing with no cross-layer tally or certifier oversight, unlike the delta pipeline which now has a certifier tracking exactly this. This upgrade brings Daily Context up to the same standard, plus adds a genuinely new capability: internal derived fallbacks so a missing external layer doesn't leave a hole in what's needed downstream.

### Requirement 1 — Revise all Daily Context steps against the NEW Board (with Underdog)
Every existing Daily Context worker was scoped/tested when Board only had PrizePicks + Sleeper. Underdog is now a real, producing source. Each Daily Context worker needs to be re-checked for any assumption tied to the old 2-source Board shape (e.g. anything reading board tables directly, anything with hardcoded source lists, anything computing "which games matter today" that might not account for Underdog's own event/team-name conventions).

### Requirement 2 — Derived (internally-calculated) fallback per layer, and it must be TEMPORARY
For each Daily Context data layer, when the miner finds partial or no real/external data for that layer on a given slate, it should compute and store an internally-derived fallback value instead of leaving a gap — using whatever real internal data we already have (season stats, recent form, established patterns, roster/depth-chart data, historical tendencies, etc. — sharpest logic available per layer, to be designed per-layer in Phase 4/5 below).
**Critical behavior, applies to every layer without exception:** a derived value is explicitly marked as derived/temporary (not real). The NEXT time the miner runs for that same slate/game/player, if real external data has since become available, the real data REPLACES the derived value. Derived is always provisional, real always wins the moment it's available. This must be a clean, uniform, reusable pattern — not reinvented per-layer ad hoc.

### Requirement 3 — A certifier, modeled on the delta certifier, but for Daily Context
The delta certifier (see PRIOR SESSION'S WORK above) tracks calendar/tally state for the classification/baseline pipeline. Daily Context needs the equivalent: a certifier that works off the **current slate** (not a rolling season window — Daily Context is inherently "what's happening around today/tomorrow's games," not historical) and keeps a **tally per layer** of: real/external data present, derived/fallback data present, partial coverage, complete coverage, and whether a given row is currently temporary (derived) or has since been confirmed with real data. It controls/reports this fresh every run — this is the mechanism that lets miners know what's still a gap and what's already covered (derived or real).

### Requirement 4 — Slate shape complexity the certifier must handle
A "slate" for Daily Context purposes can be: entirely today's games, split across today AND tomorrow (a real, common MLB scheduling pattern — e.g. some games start very late today, effectively becoming "tomorrow's slate" for context-mining purposes, or a genuine day-after slate already needs pre-mining), or entirely tomorrow's games (e.g. an off-day today). The certifier's calendar/tally logic must correctly identify and handle all three shapes — this is NOT a simple "today's date" lookup like some of the delta pipeline's simpler mechanisms; it needs the same kind of real calendar-awareness that the Board's Score Prep step already has (official game date/time resolution, not source-reported timestamps).

### Requirement 5 — Miners must be adjusted to consume the certifier's gap/tally output
Currently each Daily Context miner runs on its own. After this upgrade, miners need to read the certifier's current tally to know what's a real gap (needs real mining attempt), what's currently derived (safe to leave as-is unless a real mining attempt is due), and what's already complete/real (skip). This is the same coordination model as the delta pipeline's certifier-driven gap detection, adapted to Daily Context's per-slate (not per-season) scope.

### Requirement 6 — Downstream contract: every layer must carry what later phases need
Every Daily Context layer's real, derived, and partial output must include whatever fields the NEXT phases genuinely need: prop factor miner, matrix, enrichment, final hit-probability calculation, final score, final board. This needs to be checked per-layer as each one is revised (Phase 4/5 below) — don't assume the current output shape already covers this; verify against what those later phases actually read (many of those phases haven't been touched yet this cycle, so this may mean designing forward for contracts that don't fully exist yet — flag explicitly wherever a downstream consumer's real needs aren't yet knowable and a reasonable placeholder/superset of fields is being used instead).

### Requirement 7 — Research: are there additional daily-context data types worth mining?
Open research question, explicitly requested: beyond the current layers (weather, lineups, probable pitchers, bullpen availability, player availability, game status, schedule), are there other real, useful daily-context data types that would make the eventual hit-probability calculation more reliable/realistic/assertive? Use real external research (reliable, established sources/methodologies) to find and evaluate candidates, then lock a decision (adopt + build, or explicitly decline with reasoning) before moving to implementation for any newly-adopted layer.

### Requirement 8 — Data retention: single active set only, no base/history, expired-game purge
Daily Context tables should hold exactly one current, active set of data — no historical/base table needed (unlike classification_v6/baseline_v6 which are intentionally historical). When a game's window has passed (game is final/expired), that game's Daily Context rows must be purged, not retained. The certifier for each specific layer's worker owns this purge decision/logic (find the best trigger — e.g. tied to the same official-game-status/calendar-verified mechanism Score Prep already uses — don't invent a separate, parallel notion of "expired" if a reliable one already exists elsewhere in the system).

### Requirement 9 — Lock the derived/partial/no-data logic per layer
Whatever fallback logic gets designed per layer (Requirement 2) needs to be a deliberate, researched, "locked" (finalized, not tentative) piece of logic — same bar as the delta pipeline's calibration work. Use external research where it strengthens the approach (established sabermetric/forecasting methods, reliable public methodologies) rather than inventing something untested from scratch.

### Phases, in the order Rodolfo specified (do not reorder without a real reason, and log it here if you do)
1. **System and layer recognition** — read the real, current code for all Daily Context miner workers, the orchestrator's Daily Context stage/chain logic, and the existing `alphadog-v2-daily-certifier.js`. Understand what exists today before changing anything.
2. **Research and lock additional external daily-context data candidates** (Requirement 7) — decide what, if anything, gets added as a new layer before revising the existing ones, since a new layer may affect how the certifier's tally/schema is designed.
3. **Revise/adjust the certifier to run FIRST** in the chain, owning the tally/calendar per layer and per data-level (external/derived/partial/complete/temporary) for the current slate (Requirements 3-4).
4. **Work the existing miner workers one by one**: revise each against the new Board/Underdog reality (Requirement 1), wire it to read/write the certifier's tally (Requirement 5), design and build that layer's specific derived fallback (Requirement 2, researched per Requirement 9), confirm its output carries what downstream needs (Requirement 6). Test each worker individually before moving to the next — do not batch multiple untested workers together.
5. **For any new layer(s) locked in step 2**: build the mining logic + worker + fallback logic for each, same rigor, same individual testing, before integrating into the full chain.
6. **Adjust the certifier to run a FINAL pass** at the end of the chain too — closing out and noting remaining gaps/conditions so the next run picks up correctly (mirrors the delta certifier's `final_check` role).
7. **Assemble the full run**: certifier at the beginning (tally/gap detection) AND the end (final pass/close-out), all revised/new miners in between, tested as a whole chain, not just individually.

### DAILY CONTEXT — PROGRESS LOG (update this as each phase actually completes; do not mark anything done here until it's been verified against real data, not just deployed)
- Phase 1 (recognition): COMPLETE, verified.
  - Real architecture mapped: master chain is `board_full_run` → `daily_context_full_run` → `market_scoring_full_run`. Daily Context's 7 real stages: `daily-probable-pitchers` (starters), `daily-lineups`, `daily-player-availability`, `daily-weather` (weather+roof combined), `daily-bullpen-availability`, `daily-schedule` (team schedule spot), and `alphadog-v2-daily-usage-pulse.js` which is the real Umpire Context worker (`job_key: "daily-umpire-context"`) despite its misleading filename — verify job_key/worker_name, never the filename, for this one. Certifier (`alphadog-v2-daily-certifier.js`) runs last currently.
  - Confirmed dummy/unused stubs, NOT part of the real chain: `daily-batting-orders`, `daily-confirmed-starters`, `daily-roof-status` (roof already folded into the weather worker) — do not spend time on these unless explicitly reactivated later.
  - **All 7 real workers individually verified** to read board relevance exclusively from `SCORE_DB.score_board_prepared_current` (filtered `pickable_safe=1, matchup_status='calendar_matched', player_match_status='matched'`) — none reference any raw per-source board table (PrizePicks/Sleeper/Underdog) directly. This means Requirement 1 (revise against the new Board with Underdog) carries **low risk** at the board-integration level specifically — the abstraction already exists and holds. Still worth a final live-data check once Phase 4 gets to each worker, but this is not a structural rework.
  - Real external-dependency classification per layer (relevant for Phase 2/4/5 fallback design):
    - **Bullpen Availability, Team Schedule Spot**: already 100% internally-derived (`external_sources_used: false` in both workers' own `source_stack_locked` metadata) — computed entirely from `TEAM_DB` history. Likely need little to no new fallback logic since they're already "derived" by design; may just need the tri-state tally label applied.
    - **Umpire Context**: genuinely external (MLB StatsAPI live feed + boxscore, `MLB_LIVE_BASE`/`MLB_V1_BASE`). Real fallback candidate.
    - **Weather**: external (OpenWeather primary, per `WEATHER_PRIMARY_PROVIDER` config seen elsewhere in the system). Real fallback candidate.
    - Starters, Lineups, Player Availability: external-dependency status not yet individually confirmed — check when each is revised in Phase 4.
  - Certifier's real current logic (verified by reading the deployed file): reads the same unified prepared-board table, cross-references all 7 `daily_*_current` tables, computes a per-row status (READY_FULL_CONTEXT / READY_WITH_WARNINGS / READY_PARTIAL_ENRICHMENT / BLOCKED_* / NOT_APPLICABLE_STARTED_OR_EXPIRED), writes `daily_context_readiness_current`/`_issues` rebuilt fresh each run for today/tomorrow only. Real gaps against the new spec: only binary available/missing per layer (no real/derived/partial/complete/temporary tally), no fallback-triggering logic at all, only runs last (no first-pass mode), window logic checks today+tomorrow but doesn't classify/report which of the 3 slate shapes applies.
- Phase 2 (research + lock additional daily-context data candidates): IN PROGRESS.
  - **Concrete improvement findings on the two already-derived layers** (requested explicit scrutiny, backed by real research on established bullpen/schedule fatigue methodology):
    - **Bullpen Availability — real gap found**: `closer_recent_usage_flag`, `setup_recent_usage_flag`, and `extra_innings_recent_flag` are hardcoded to `0` in both INSERT statements despite the table having real columns for them — they are never actually computed. Research (SABR, MLB fatigue-tracking literature) confirms this matters a lot in practice: elite/high-leverage relievers now absorb roughly half of all bullpen innings and pitch in save situations far more disproportionately than lower-leverage arms, so a fatigued closer/setup arm is a materially bigger signal than aggregate team pitch counts alone — the current formula treats all relievers as equivalent. Also worth adopting: real bullpen-tracking tools (e.g. recency-weighted 5-day pitch decay, weighting yesterday's load higher than 4-days-ago) are more precise than the current hard 1/2/3-day bucket thresholds (pitches1>=80/60/40, pitches2>=130). Recommendation for Phase 4 when this worker is revised: (1) identify high-leverage relievers via saves/holds in `bullpen_history` or a role field if available, actually populate the three flags, and weight the fatigue score toward them; (2) consider recency-weighted decay instead of hard day-buckets. Thresholds themselves (80/60/40/130) are a reasonable heuristic but not empirically backtested — same category of caveat as the delta pipeline's confidence-formula constants.
    - **Team Schedule Spot — logic is genuinely sound**: played-yesterday/3-in-4/4-in-6/doubleheader/venue-change/travel-distance/timezone-transition/day-after-night are all real, well-documented fatigue factors, additively scored (fatigue>=6 high, >=3 moderate). One real, secondary gap: no directional asymmetry for travel — east-to-west vs west-to-east circadian disruption is a documented, real effect in MLB travel/performance research that isn't currently distinguished (both directions currently score identically via `timezone_transition_flag`). Minor relative to the bullpen gap; worth a small enhancement in Phase 4, not a rebuild.
  - **Additional daily-context data candidates — researched and locked**:
    - **NEW LAYER CANDIDATE: Catcher Context** — locked as a real, research-backed candidate, but explicitly DEFERRED to a future phase, not built now. Feasibility and mineability (data source availability, framing-metric access) to be assessed when that future phase is reached. Do not build this in Phase 5 unless explicitly revisited and re-approved.
    - **Considered, explicitly declined for now (with reasoning, not just omitted)**:
      - Defensive positioning/shifts — real, established factor (also explicitly named by THE BAT X), but reliable shift/positioning data is Statcast-tier and not confirmed available to us. Flagged as a good future candidate, not built on an unverified data-availability assumption.
      - Game total / implied team runs (Vegas-based) — real and used by established models (Dimers explicitly cites "market odds movement"), but this is fundamentally market data. A stub already exists for this (`alphadog-v2-phase2a-game-total-context.js`) — belongs to a later Market/Enrichment stage, not Daily Context.
      - Park/weather/roof "impact" composite scoring — the existing `phase2a-park-impact.js`/`phase2a-weather-impact.js`/`phase2a-roof-impact.js`/`phase2a-run-environment.js` stubs (all currently dummy placeholders) appear to be reserved for an Enrichment-stage layer that *interprets/combines* Daily Context's raw weather/park data into impact scores — not a Daily Context mining layer itself. Worth revisiting when the Enrichment phase is reached; not in scope now.
  - Phase 2 status: COMPLETE (both the already-derived-layer scrutiny and the new-candidate research/lock).
- Phase 3 (certifier redesign): COMPLETE, verified with real data.
  - Added `data_source_level` (TEXT, default 'unknown') and `is_temporary_derived` (INTEGER, default 0) columns to all 7 real layer `_current` tables (`daily_starters_current`, `daily_lineups_current`, `daily_player_availability_current_v1`, `daily_game_weather_current`, `daily_bullpen_availability_current`, `daily_team_schedule_spot_current`, `daily_umpire_context_current`). These are the standard, uniform fields every layer will populate in Phase 4 to report real vs. derived vs. temporary status. Not yet populated by any worker (still Phase 4 work) - certifier currently reads them as 'unknown'/unclassified, which is correct and expected until each worker is revised.
  - Added two new tables to `alphadog-v2-daily-certifier.js`: `daily_context_tally_current` (per-layer, per-date real/derived/temporary/unclassified/missing counts against expected units) and `daily_context_slate_current` (per-date slate shape + game count).
  - Added `determineSlateShape()` - classifies the current slate as `same_day_only`/`split_today_tomorrow`/`next_day_only`/`no_games` from real, calendar-verified prepared-board games (Requirement 4).
  - Added `computeLayerTally()` + `writeTally()` - computes expected-vs-actual coverage per layer per date, tracking real/derived/temporary/unclassified/missing counts. Expected units differ by layer scope: game-scoped (weather/umpire/starters) expect one row per distinct game_pk; team-scoped (bullpen/schedule_spot) expect one row per team appearance (2 per game); player-scoped (lineups/player_availability) expect one row per distinct resolved player from the prepared board.
  - Added `purgeExpiredGameLayers()` - purges precisely by game_pk across all 7 real layer tables for any game that's final/postponed/cancelled or live-and-past-start, rather than relying only on each worker's own broader date-window retention (Requirement 8). A game can go final mid-window well before its date rolls out of the today/tomorrow retention window that most workers use.
  - **Verified against real, live data** (direct diagnostic dispatch, not just deploy success): correctly classified `same_day_only` for a real 7-game slate, computed correct expected-unit counts per layer (7 games for game-scoped layers, 14 team-slots for team-scoped, 159 player-slots for player-scoped layers), and ran the purge logic without error (found and attempted purge on 1 real expired game_pk).
  - Added the certifier as a stage at the START of `DAILY_CONTEXT_FULL_RUN_STAGES` too (`daily_context_certifier_first_pass`), not just the end (`daily_context_certifier` unchanged) - Requirement 3/6 (certifier runs first and last). Same worker/job_key reused for both, since the certifier's full computation is cheap and idempotent to run twice - no special first-pass/last-pass mode was needed.
  - **Real risk checked and confirmed safe before deploying**: two stages in the same chain now share `job_key: "daily-certifier"` - the exact class of bug that caused real cross-matching problems earlier this session in the delta pipeline (classification/HP sharing a job_key). Verified the daily-context-full-run child-matching logic (`childStageKey === stage.stage_key` checked first, job_key+worker_name only as a fallback when stage_key is empty) combined with the fact that `dailyContextFullRunChildInput` unconditionally stamps `stage_key` for every child (not a retry-only path that could bypass it, unlike the earlier delta-pipeline bug) - confirmed the two certifier stages will always be correctly distinguished.
  - One real, caught-before-deploy mistake: a comment accidentally used `#` instead of `//` mid-block, which would have been a JS syntax error. Caught via the deploy failing immediately (GitHub Actions build), fixed in the next commit before any further work continued. Worth remembering: multi-line `//` comments need every line prefixed correctly, no shortcuts.
- Phase 4 (work the 7 workers one by one): IN PROGRESS. Order: Starters → Lineups → Player Availability → Weather → Bullpen Availability → Team Schedule Spot → Umpire Context.
  - **Starters (`alphadog-v2-daily-probable-pitchers.js`): DONE, verified with real data.**
    - Board/Underdog compatibility: already confirmed safe in Phase 1 (reads unified `score_board_prepared_current`).
    - Derived fallback built: `deriveLikelyStarter()` predicts the likely starter from the team's own real `TEAM_DB.starter_history` rotation data when no official probable/actual pitcher exists yet - excludes anyone under 3 days rest, prefers whoever's rest gap is closest to the standard 5-day rotation cycle. Explicitly low-confidence (`LOW_DERIVED_ROTATION_PREDICTION`) and marked `data_source_level='derived'`, `is_temporary_derived=1` - always overwritten by real data the moment MLB StatsAPI reports an official probable pitcher (same `current_key` upsert, no separate logic needed for the "temporary" replacement behavior - it falls out naturally from the existing INSERT OR REPLACE pattern).
    - Wired to the new tally columns: real official/actual starters get `data_source_level='real'`; derived predictions get `'derived'`; genuine TBD-with-no-derivable-history gets `'unknown'`.
    - **Real bugs caught and fixed during this one worker** (documented so the pattern is recognized faster next time): (1) a `#` vs `//` comment typo caused a deploy failure, caught by the GitHub Actions build failing immediately; (2) a function signature line was accidentally dropped in a patch, leaving its body orphaned outside any function - a real structural bug, not just a comment issue, caught the same way; (3) an INSERT statement had a placeholder-count/column-count mismatch (`D1_ERROR: 42 values for 41 columns`) caught only by actually running the worker against real data, not by deploy success - fixed by rebuilding the VALUES clause with an exact, bash/python-verified placeholder count before redeploying. None of these reached a state where they could have corrupted data; all were caught before or immediately upon real execution.
    - **Verified with a real live dispatch**: 62 teams checked, 0 TBD, 57 real official starters confirmed, 5 genuine derived predictions used (including real, plausible MLB names - e.g. Jacob deGrom for the Rangers), 0 blocking rows. Confirmed directly in `daily_starters_current` that all 5 derived rows carry the correct status/confidence/data_source_level.
  - **Lineups (`alphadog-v2-daily-lineups.js`): DONE, verified.**
    - Derived fallback built: `deriveLineupFromRecentGame()` uses each team's most recent completed game's real batting order (from `STATS_HITTER_DB.hitter_game_logs`) as the prediction when no official lineup is posted yet - the same heuristic real commercial lineup-projection tools use. Wired into the main per-game loop: only queried when a side's `lineup_status !== "posted_lineup"`; `collectLineupWriteRows` extended to accept both `posted_lineup` (real) and `derived_likely_lineup` (derived) statuses, correctly tagged with `data_source_level`/`is_temporary_derived`. Real rows always win on the next run via the same `${gamePk}_${side}_${slot}` upsert key.
    - **Real, substantive bug caught by checking actual data values, not just query success**: MLB's `batting_order` field is a 3-digit code (`slot*100 + substitution_index`, e.g. 300 = slot 3 starter, 301 = slot 3's in-game substitute) - not a plain 1-9 integer. My first version stored the raw 3-digit value directly as the lineup slot, which would have silently written wrong/nonsensical slot numbers. Caught by inspecting real query output (saw values like 100, 301, 501, 900 instead of 1-9) before it ever reached a live run. Fixed by dividing by 100 and deduping to the primary (first/lowest) entry per slot. Verified correct against the real 11-row sample that exposed the bug: correctly collapses to exactly 9 slots, each keeping the original starter over the substitute.
    - Also caught and fixed: a `ReferenceError` (used an undefined `todayStr` variable instead of `retentionDatesToKeep()[0]`), and confirmed `STATS_HITTER_DB` was already bound at the infrastructure level for this worker despite being missing from its own health-check list (added it there for correctness).
    - **Verified with a real live dispatch**: 7 games, 126 rows written, all `data_source_level='real'` this run (every side already had an official posted lineup at test time, so derived correctly did not engage - confirmed this is the right behavior, not a bug, since real always takes priority). Derived code path verified correct via direct data simulation given live conditions didn't happen to exercise it during testing.
  - **Player Availability (`alphadog-v2-daily-player-availability.js`): DONE, verified.**
    - Real, valuable finding before building anything: this worker already has a sophisticated multi-tier fallback chain (active roster → 40-man-active-fallback if active source fails → injured list → transaction inference → 40-man-not-active → source_missing as last resort). Checked real current data first: genuine unresolved gaps (`source_missing`) occur **zero times** in practice - the existing chain already works well. This meant the right scope here was smaller and more surgical than Starters/Lineups, not a from-scratch rebuild.
    - Added one more real derived tier for the rare true `source_missing` case: `loadRecentAppearances()` bulk-checks both `STATS_HITTER_DB.hitter_game_logs` and `STATS_PITCHER_DB.pitcher_game_logs` for real appearances in the last 10 days - a player who's genuinely played for this team recently is strong corroborating evidence they're still active despite a roster-endpoint gap. Wired into both `source_missing` branches in `classify()`.
    - Tagged `data_source_level`/`is_temporary_derived` across all existing branches: `real` for genuine official-source hits (active roster, IL, blocking transactions, 40-man-not-active), `derived` for the two inference tiers (`forty_man_active_fallback` and the new `derived_recent_appearance`), `unknown` only for the true unresolved `source_missing` case.
    - **Verified with a real live dispatch**: 159 players checked, 0 blockers, all 159 rows correctly tagged `data_source_level='real'` this run (every player had a genuine active-roster hit, so neither derived tier was needed - confirmed this is correct behavior given the real data showing gaps are already rare, not a sign the new code doesn't work).
  - **Weather (`alphadog-v2-daily-weather.js`): DONE, verified.**
    - Confirmed weather itself genuinely cannot and should not be derived (no internal data substitutes for actual atmospheric conditions) - correctly out of scope by design, per explicit direction.
    - Verified the existing 2-tier external fallback (OpenWeather primary → Open-Meteo free/no-key fallback on failure, both merged with MLB's own live-feed weather as a third independent signal) is already correctly wired and, per real current data, succeeding 100% of the time via the primary provider. A third "optional" provider (WeatherAPI) was found configured in env vars but never actually implemented in code - confirmed low value to build given the existing chain's real reliability, left as a noted future option rather than built.
    - **Real, separate gap found and fixed**: retractable-roof stadiums (Rogers Centre, Globe Life Field, T-Mobile Park, etc.) had no live open/closed roof source at all - the code explicitly admitted this. Roof status is a stadium-operations *decision* (not weather itself), so added a real, defensible derived inference: teams generally close for meaningful rain risk or temperature extremes and open for pleasant conditions - uses real forecast data already fetched (precipitation probability, temperature), not invented weather. Explicitly low-confidence and marked derived/temporary.
    - Wired `data_source_level`/`is_temporary_derived`: `real` for any live-sourced weather or genuinely-known indoor/dome status, `derived` for the new roof inference, `unknown` only for the true no-data case.
    - **Verified with a real live dispatch**: `roof_unknown_count` went from 5/15 (in the prior real batch) to 0/7 this run. Directly confirmed in the database: Globe Life Field (a real retractable-roof stadium) correctly showed `derived_likely_open` with `data_source_level='derived'` given real forecast conditions (92°F, 0% precipitation); all 6 outdoor games correctly showed `data_source_level='real'`.
  - **Bullpen Availability (`alphadog-v2-daily-bullpen-availability.js`): DONE, verified.**
    - Fixed the real gap identified in Phase 2: `closer_recent_usage_flag`/`setup_recent_usage_flag` were hardcoded to `0` in both INSERT sites despite real columns existing for them. `TEAM_DB.bullpen_history` already has real `saves`/`holds` columns per appearance - used these directly (a save/hold recorded in the last 2 days is a factual signal of high-leverage usage, not an inference) to compute genuine per-team closer/setup flags, feeding into both the issue log and the fatigue score (closer usage +12, setup usage +6).
    - This layer is 100% internally-computed by design (`external_sources_used: false` - no external source it stands in for), so `data_source_level` is uniformly `'real'` here, not a derived/temporary fallback - it's the primary, authoritative computation for this layer.
    - **Verified with a real live dispatch**: 14 teams checked, 9 showing genuine, differentiated closer/setup usage (real teams like the Cardinals, Braves, Blue Jays correctly flagged) - previously this was always 0 for every team regardless of real save/hold activity.
  - **Team Schedule Spot (`alphadog-v2-daily-schedule.js`): DONE, verified.**
    - Fixed the real gap identified in Phase 2: travel direction (east vs. west) was previously weighted identically. Researched and applied the actual canonical MLB-specific study (Song/Allada et al., Northwestern/PNAS 2017, 20 years of real MLB data): jet-lag effects are concentrated in eastward travel with limited effects after westward travel (consistent with the >24h human circadian period), and the effect is strongest for the home team's offense specifically after returning from a road trip. Added `eastward_travel_flag`/`westward_travel_flag` (derived from the already-computed UTC offset comparison), weighted eastward higher (+2) than westward (+1) in the fatigue score, with an extra +1 when combined with returning home - directly matching the research's specific finding.
    - This layer is also 100% internally-computed by design, so `data_source_level` is uniformly `'real'`.
    - **Verified with a real live dispatch**: 14 teams checked, 0 errors. Cross-checked the new eastward/westward flags against the pre-existing `timezone_transition_flag` (unrelated to this fix) - both showed the identical all-zero pattern for today's specific slate (no team happened to be starting a new-timezone road trip today), confirming the new logic is consistent and correctly computing, not a bug - just a real, quiet day for this particular signal.
  - **Umpire Context (`alphadog-v2-daily-usage-pulse.js`): DONE, verified. Phase 4 now COMPLETE (7 of 7 workers).**
    - Confirmed via real earlier-session data that this layer's official pregame source essentially never resolves (100% `no_official_pregame_source` in an earlier real batch) - a strong candidate for a real derived fallback. Confirmed no internal historical umpire data existed anywhere in the system before this work (checked directly).
    - Researched real, established MLB umpiring practice before building anything: a four-man crew works an entire series together, rotating positions daily (well-documented, e.g. today's 1B umpire works the plate tomorrow). This makes a recent real HP assignment for the same home venue a genuine, if imprecise, low-confidence predictor when today's official assignment hasn't posted yet.
    - Built a narrow, purpose-built `daily_umpire_assignment_history` table (10-day bounded retention) specifically to support this derivation - a deliberate, scoped exception to the single-active-set rule, justified because the general certifier-owned retention (today/tomorrow only) would otherwise destroy the very history the derivation needs before it could ever be useful. Real assignments are recorded here whenever found; the derivation checks the last 4 days for this same home venue when no official source exists.
    - Wired `data_source_level`/`is_temporary_derived`: `real` for genuine official assignments, `derived` for the new crew-based prediction, `unknown` for the true no-data case.
    - **Verified with a real live dispatch**: all 7 games showed real official assignments this run (games had progressed further since the earlier-session check that found 100% missing) - confirmed correct, no regression (`data_source_level='real'` for all 7), and confirmed the history table now holds 7 real, named MLB umpire assignments that will genuinely support the derivation the next time an official source isn't yet available for these same venues.
  - **Phase 4 complete.** All 7 workers (Starters, Lineups, Player Availability, Weather, Bullpen Availability, Team Schedule Spot, Umpire Context) revised for the new Board/Underdog reality, wired to the certifier's real/derived/temporary/unclassified/missing tally, and given real, researched derived fallbacks where a genuine gap existed - each verified against live data, not just deploy success.
- Phase 5 (deferred candidate revisited): **Catcher Context - DONE, verified, added into the existing Lineups worker (`alphadog-v2-daily-lineups.js`) rather than a new worker, per explicit instruction.**
  - **Assignment**: derived directly from data the Lineups worker already parses - the catcher is simply the posted-lineup player with position code "2". No new source needed.
  - **Framing quality + pop time**: confirmed genuinely feasible via real research and a real diagnostic test (not assumption) - Baseball Savant (`baseballsavant.mlb.com`) is MLB's own official Statcast portal (not a third-party scrape target) and exposes season-level leaderboards as direct CSV downloads. A live server-side fetch confirmed clean, real CSV data with MLB player IDs matching our own system exactly (no ID-mapping work needed) - no paywall blocking despite an ambiguous `meta-paywall` page flag that raised a fair question beforehand.
  - New tables: `REF_DB.ref_catcher_framing_poptime` (season-level, refreshed only when >20h stale since these are slow-changing aggregates - avoids hammering Savant every run) and `DAILY_DB.daily_catcher_context_current` (per-game, joins the daily catcher assignment to the reference metrics).
  - **Two real bugs found and fixed along the way, neither caused by this feature but blocking it**: (1) the orchestrator's dispatch code called `isServiceBindingTimeoutLike()` for daily-lineups' error-recovery path, but that function was never defined anywhere in the 18,810-line orchestrator file - a genuine latent bug, fixed using the same timeout-keyword pattern already established elsewhere in that file. (2) `alphadog-v2-daily-lineups.js` had no `first()` helper (only `all()`/`execRun()`), which new code needed - added it. Also caught and fixed a real ordering bug in the new code itself: the reference-data map was being loaded before the refresh call that populates it, silently returning empty results despite the underlying fetch succeeding.
  - **Verified with real live dispatches**: all 14 catchers correctly identified by name (Realmuto, Rutschman, Kirk, McCann, etc.), 13 of 14 showing real, differentiated framing/pop-time metrics matching known real-world reputations (Rutschman's elite +5.08 framing runs, Kirk's high 50.6% strike rate). The one gap (a backup catcher below Savant's qualifying pitch threshold) is an honest, correct absence, not a bug.
- Phase 6 (certifier final pass - close/note gaps for next run): EFFECTIVELY COMPLETE as part of Phase 3's design. The certifier already runs as both the first AND last stage in the chain (`daily_context_certifier_first_pass` and `daily_context_certifier`, same worker/job_key, both wired into `DAILY_CONTEXT_FULL_RUN_STAGES`). Since the certifier's tally/gap computation is cheap, idempotent, and re-reads live sidecar state fresh every time, the "last pass" run naturally closes/notes whatever gaps remain after all 7 miners have run in this same chain execution - no separate first-pass/last-pass logic branch was needed or built. See "FULL RUN LIVE VERIFICATION" section below for confirmation this actually works in a real chain execution, not just in isolation.
- Phase 7 (full run assembly, certifier at start+end with all workers wired in): IN ACTIVE LIVE VERIFICATION as of this update - see "FULL RUN LIVE VERIFICATION" section below for the detailed real-execution status. Structurally complete (9-stage chain confirmed correct in code); a live end-to-end test was in progress with 5 of 9 stages confirmed completed with real data at the time of this handoff update, continuing via real backend cron/self-continuation infrastructure.

## CRITICAL BUG FOUND AND FIXED — CERTIFIER'S PURGE LOGIC WAS WIPING EVERY LIVE GAME'S DATA (found by a follow-up session doing exactly what this handoff asked - verify before trusting state, don't assume)
A follow-up session correctly did NOT trust the full-run "completed" status at face value - it re-queried the actual layer tables live and found `daily_lineups_current`, `daily_player_availability_current_v1`, `daily_game_weather_current`, and `daily_bullpen_availability_current` completely empty (0 rows), despite the certifier's own tally moments earlier showing real coverage, and no logged job having run in between. This was investigated to root cause rather than guessed at, and the cause was severe:

`isGameStartedExpiredOrUnavailable()` in `alphadog-v2-daily-certifier.js` (built in Phase 3) checked `(is_live === 1 AND gameHasReachedStart(game))` as one of its "expired" conditions. But `gameHasReachedStart` just checks whether the game's scheduled start time has passed - which is true of **every single live game by definition** (a game cannot be live before it starts). So the moment any of the day's games actually went live (evening, real games in progress), the certifier's own last-pass purge - which runs as part of the SAME job invocation that computes the tally - treated every one of those games as "expired" and wiped its rows from all 7 layer tables in one pass. This is why it never surfaced in earlier testing this session (all prior tests ran while games were still scheduled/pregame, never live) and why `daily_starters_current` still had partial rows (only the not-yet-live games survived).

**Fixed**: removed the live-game branch entirely. A game is only genuinely expired (Requirement 8's actual intent - "a game whose window has passed") when it's final/postponed/cancelled - a live, in-progress game is the opposite of expired, it's exactly when this data matters most. `gameHasReachedStart` itself was kept as a small, clearly-commented, purely-informational helper (used only in a diagnostic `details_json` field, never in any purge/expiry decision) since removing it outright broke a real call site and had to be re-added.

**Verified fixed against the real failure condition, not just deployed**: re-populated lineups (126 rows), ran the certifier again while tonight's games were genuinely still live, confirmed `purged_game_count: 0` and all 126 lineup rows survived. This is the same rigor bar as everything else in this document - a deploy succeeding was not treated as proof; the exact scenario that caused the bug was re-run and confirmed fixed.

**If you are a new session reading this**: if you ever see Daily Context layer tables mysteriously empty with no corresponding job in `control_job_queue`, check the certifier's purge logic FIRST before assuming a miner bug - the certifier's purge runs silently as part of its own regular tally computation, not as a separately-logged action, so an over-aggressive purge condition can look exactly like "nothing ran but data vanished."
Rodolfo explicitly asked for one more deep research pass to find any additional real, reliable, repeatable, easy-to-mine factor beyond what Daily Context already covers, excluding market/odds (that's a later phase). This was answered thoroughly, not assumed:
- **Checked against multiple independent professional prop-betting/analytics sources** (FTA, SportsGrid, BettingPros, a sportsbook prop-pricing explainer, MLB betting-model overviews): every factor they consistently cite as real and used, we already have covered somewhere in the system - park factors, weather, umpire tendencies, bullpen workload, lineup position/matchups, platoon splits (apparently already live elsewhere as `base-hitter-splits`), recent form (apparently already live as `PHASE2B_RECENT_FORM_WORKER`).
- **One narrower, real gap found and explicitly NOT recommended as a priority**: pitch-count limits for starters recently returning from the IL (e.g. "75-pitch limit after injury") is a real factor multiple sources mention, but it's harder to source reliably/repeatably than anything else in scope - flagged, not built.
- **The one real, valuable finding, which led directly to Catcher Context (Phase 5) being un-deferred and built**: researching catcher framing specifically surfaced that Baseball Savant (`baseballsavant.mlb.com`) - MLB's own official Statcast portal, not a third-party scrape target - exposes season-level leaderboards as direct, clean CSV downloads. This was NOT assumed to work; a real diagnostic fetch was built and run from an actual Cloudflare Worker (search-tool fetches can't reliably simulate a genuine server-side request) before committing to building on it. The real fetch returned clean CSV with MLB player IDs matching our own system's IDs exactly (e.g. J.T. Realmuto = 592663 in both), meaning no ID-mapping work was needed. A `meta-paywall: true` flag on the page had raised a fair concern beforehand; the real test proved it wasn't actually blocking access to this data.
- Conclusion delivered to Rodolfo: nothing else worth adding right now beyond Catcher Context (built, see Phase 5 above). Market/odds-adjacent factors (game totals, implied team runs) were already correctly identified in Phase 2 as belonging to a later Market/Enrichment phase, not Daily Context.

## RE-MINING / TEMPORARY-FALLBACK FRESHNESS GUARANTEE — VERIFIED BY CODE INSPECTION, NOT ASSUMED
Rodolfo raised a critical correctness concern: if a miner runs at 9am and mines/derives data for a 7pm game, then runs again at 5pm, it must re-attempt the real source fresh every time - never treating an earlier derived/temporary value as settled, since real data (a posted lineup, updated weather, etc.) may now exist. This was verified with actual code inspection across the three most complex derived-fallback workers (Starters, Lineups, Weather): **zero instances** of any "skip if already exists" / "reuse cached derived value" anti-pattern in any of them. Every single invocation re-fetches/re-checks the real external source from scratch; a derived fallback is only ever computed fresh, in that same run, if the real source still comes up empty that run. The `INSERT OR REPLACE` upsert pattern used everywhere means real data automatically overwrites a prior derived row the instant it becomes available on a later run - this is a structural guarantee from the original design, not a lucky accident, and it was confirmed to hold across every layer built in Phase 4/5.

## CATCHER CONTEXT FRAMING QUALIFYING THRESHOLD - NOTE FOR ANYONE LOOKING AT `metrics_available_flag=0` ROWS
Baseball Savant's framing/pop-time leaderboards only include catchers who've received a minimum number of tracked pitches/attempts that season. A backup catcher with limited playing time will legitimately show `metrics_available_flag=0` in `daily_catcher_context_current` - this is correct, honest behavior (the metric genuinely doesn't exist yet for that player this season), not a bug or a gap to chase. Do not build a fallback/derivation for this specific case without discussing it first - a crude proxy would be worse than an honest absence here.

## CONTROL ROOM / ORCHESTRATOR PARITY AUDIT - REAL GAPS FOUND AND FIXED (explicit ask: "check control room worker and html parity, same for the orchestrator")
Three real files govern what a user sees/triggers: `alphadog-v2-orchestrator.js` (the actual stage-chain execution logic - source of truth for what runs and in what order), `alphadog-v2-control-room.js` (the deployed Cloudflare Worker serving the dashboard, with its own embedded copy of the HTML plus all the `orchestrator_enqueue_*` job-creation handlers), and `control_room.html` (a static fallback copy of the same dashboard HTML, kept in the repo for a "safe route restore" scenario per its own debug-config comment).

**Verified correct, no changes needed:**
- `DAILY_CONTEXT_FULL_RUN_STAGES` in the orchestrator (the actual execution order): confirmed correct - certifier-first-pass, then the 7 miners (starters, lineups, player_availability, weather_roof, bullpen, team_schedule_spot, umpire), then certifier-last. 9 stages total, in the right order, each `stage_key`/`job_key`/`worker_name` correctly mapped. Catcher Context correctly has no separate stage entry since it rides inside the Lineups stage, as intended.
- The `orchestrator_enqueue_daily_context_full_run` handler in `control-room.js` correctly creates a `job_key: 'daily-context-full-run'` parent row, which the orchestrator's own (confirmed-correct) stage-processing logic picks up - meaning the actual "Full Run" button mechanism was never broken; only its own internal bookkeeping text was stale (below).

**Real, confirmed gaps found and fixed:**
1. **`control-room.js`'s own `approved_chain_order`/`approved_stage_order` arrays and human-readable response note text for `orchestrator_enqueue_daily_context_full_run`** were stale - still listing only the old 8-stage chain (Starters through Context Cert), with no mention of the certifier-first-pass added back in Phase 3. This didn't break execution (the orchestrator's real stage array was already correct and is what actually runs), but it meant anyone reading the button's own response/logs would see an inaccurate description of what was about to happen. Fixed: both arrays now correctly list all 9 stages including `daily-certifier`/`daily_context_certifier_first_pass` at position 0, and the note text now explicitly describes "Context Cert (First Pass - finds gaps) ... Context Cert (closes gaps)" plus a mention that Catcher Context rides within the Lineups stage.
2. **`control_room.html` (the static fallback file) was one full version behind the actual deployed worker** - stamped `v1.6.214-stateful-baseline-v5-delta-chunked-continuation` while the real worker was already at `v1.6.215-baseline-v5-classification-rescue-target-batch`. This is a genuine, pre-existing parity drift (not something introduced this session) between the two supposedly-identical HTML sources. The actual DAILY JOBS button list itself was confirmed identical between the two files (same 10 buttons, same job names) - only the version-tracking strings had drifted. Fixed: all 3 version-tag occurrences in `control_room.html` (the HTML comment, the `#versionTag` div, and the `debugConfig()` JS string) now match the deployed worker.

**A separate, real, unrelated bug found while testing the Full Run button's actual execution path** (not a parity issue, a genuine latent defect): the orchestrator's dispatch code calls `isServiceBindingTimeoutLike()` inside the daily-lineups error-recovery path, but that function was never defined anywhere in the entire 18,810-line `alphadog-v2-orchestrator.js` file - confirmed via a direct grep showing exactly one match (the call site, no definition). This had presumably never been hit before (the daily-lineups error-recovery branch is only reached on specific dispatch-exception conditions), but it was a real, reproducible crash the moment it was hit during this session's testing (`"error": "isServiceBindingTimeoutLike is not defined"`). Fixed by adding the missing function using the same timeout-detection keyword pattern already established elsewhere in the same file (checks for `service_binding_timeout`, `worker_dispatch_exception`, `timeout_after_`, `aborterror`, `network`, `temporar`, `timeout` substrings, case-insensitive).

## FULL RUN LIVE VERIFICATION - IN PROGRESS AS OF THIS UPDATE, REAL EXECUTION, NOT A DRY RUN
A genuine end-to-end test of the Daily Context Full Run chain was launched exactly the way the Control Room's "DAILY JOBS > Full Run" button does it (a `job_key: 'daily-context-full-run'` parent row inserted into `control_job_queue`, then the orchestrator woken/pumped). This is real, live infrastructure execution against real data, not a simulation.

**Confirmed completed stages, in order, each verified against real output (not just "completed" status):**
1. `daily_context_certifier_first_pass` - completed. Real output confirmed a genuine per-layer, per-date tally: e.g. lineups showed 119 real/40 missing at that moment (reflecting whichever sidecar batches existed at the time from prior standalone testing this session, which is expected/correct behavior for a first-pass read), weather showed 6 real/1 derived, bullpen/schedule_spot/umpire/starters all showed complete real coverage. This is the certifier's tally mechanism genuinely working against live state, not a hardcoded/fake response.
2. `daily_starters` - completed. Real output: 60 real + 2 derived starters this run (a different real split than earlier standalone tests, since real conditions shift run to run - expected).
3. `daily_lineups` - completed. 126 rows written.
4. `daily_player_availability` - completed (on the SECOND attempt - see below).
5. `daily_weather_roof` - completed.

**A real, useful observation about the self-healing infrastructure, caught live during this test**: the first `daily_player_availability` attempt got stuck in `running` status for several minutes with no progress (a real stall, not an artifact of testing). The system's own stale-running-job recovery mechanism detected this and automatically spawned a fresh retry attempt under a new request ID, which then completed successfully. This is the SAME kind of "stale running recovery" pattern already documented for base-hitter-game-logs etc. elsewhere in this system, and it was directly observed working correctly for Daily Context Full Run specifically for the first time this session. The stuck original attempt was correctly marked `failed` (not left hanging indefinitely), and the parent chain correctly moved forward once the retry succeeded.

**Remaining stages not yet confirmed complete as of this handoff update**: `daily_bullpen_availability` was showing a similar "stuck running" pattern (no progress for 10+ minutes) at the time this update was written, with the same stale-recovery mechanism expected to eventually detect and retry it, followed by `daily_team_schedule_spot`, `daily_umpire_context`, and the final `daily_context_certifier` pass. **If you are a new session picking this up: check `control_job_queue` for `job_key='daily-context-full-run'` and its child stages first, before assuming this needs to be re-launched from scratch.** A simple `SELECT request_id, job_key, status, updated_at FROM control_job_queue WHERE created_at >= '<test start time>' ORDER BY updated_at DESC` will show exactly where it landed. If the parent chain is still `pending`/`running`, it likely completed on its own via backend cron continuation (this system deliberately does NOT require the browser/session to stay open - "browser may close now" is a real, correct part of the design) - no action needed beyond checking. If it shows `failed` with no further retries, that's the one scenario actually worth investigating fresh.

**A real, useful pattern observed about this system's self-continuation timing, worth knowing before assuming something is broken**: after each manual `orchestrator_tick` wake, the response often reports `self_continue_scheduled: true` with a short delay (e.g. 2500ms), meaning the backend schedules its OWN next continuation automatically - but this genuinely takes real wall-clock time (the `GLOBAL_ORCHESTRATOR` lock is held for up to 5 minutes per acquisition), and repeatedly manually re-pumping while a real background cycle is already holding the lock just returns `lock_busy` (correct, safe behavior preventing double-processing, not a bug). The right approach when this happens: wait a meaningful amount of time (60-120+ seconds) and re-check via a lightweight SQL status query rather than repeatedly re-pumping - matches the D1/worker-timeout-awareness lesson already documented in "HOW TO WORK WITH THIS PERSON" below.

## UPDATE 2026-07-12 (LATER) — FINAL SCORING SYSTEM: FULL SCOPE LOCKED BEFORE ANY IMPLEMENTATION

Rodolfo has now scoped the entire remaining pipeline: Prop Factor Miner → Matrix Builder → Enrichment → Final Hit Probability → Final Score → Final Board. This section captures his full requirements verbatim/faithfully, in full, before any code is touched — same discipline as the Daily Context spec above. If you are picking this up: read this whole section before assuming any phase below is done just because a later note says "in progress."

### Phase order (do not reorder without a real reason, log it here if you do)
1. Prop Factor Miner — pre-existing, built by a different AI assistant (ChatGPT). Rodolfo is explicit he doesn't know what it does or whether it's still needed. Must be mapped/understood first. Only keep/adjust if still necessary given the system's real upgrades since. If kept, must cover all prop-line expansion plus the new Board and every other adjustment made this session.
2. Matrix Builder — same treatment: pre-existing ChatGPT-built, understand first, adjust or retire only if justified.
3. Enrichment — the deep, critical phase (full spec below).
4. Final Hit Probability calculation.
5. Final Score.
6. Final Board.

### Enrichment — full requirements (the most complex phase, needs the most research)
Purpose: works like classification_v6/baseline_v6's tiering approach, but for how a specific factor influences a specific leg. Needs deep, locked research to establish the right number of profiles/tiers — same rigor as classification_v6's z-score tiering work.

**Required dimensionality — no flattening, no summarizing, fully microscopic:**
- Split by player tier (multiple tiers, not just 2-3).
- Split by prop line.
- Split by variation/direction of that prop line (over/under, alt lines, etc.).
- Split by each raw factor (weather, lineups, umpire, bullpen, park, schedule spot, catcher context, etc. — all daily-context and market-context factors, game-level and player-level).
- Split by the specific factor's own variation (e.g. weather: rainy/windy at multiple intensity levels, very warm, humid, dry, etc. — not just "bad weather/good weather").
- Cross-product: factor × variation × player tier × prop line × prop-line variation/direction. Every leg of the cross-product needs its own locked logic — not a shared/generic bucket.

**Reliability/confidence weighting requirement:**
Every enrichment factor's influence must be weighted by the reliability of the underlying data feeding it: how much data is available for that specific factor, how fresh it is, whether it's real or derived (ties directly into the `data_source_level`/`is_temporary_derived` tri-state built into Daily Context and Market Context this session), and any other data-quality dimension that matters. This weighting affects both confidence and hit-probability percentage. Needs real, researched logic and weights (external research into established reliability-weighting/forecasting methods, not invented from scratch), and an explicit decision on WHERE the weighting applies — enrichment itself, or final hit-probability calculation — that decision must be made and recorded, not left ambiguous.

**Performance/complexity mitigation — the "preset dictionary" approach:**
Given the true scale of factor × variation × tier × prop-line × direction is enormous, Rodolfo's explicit design: pre-build a full, locked framework covering every possible scenario in advance (a big one-time research/design effort), rather than calculating novel logic per-leg at runtime. Concretely:
- For each raw factor, enumerate every real variation it can take (e.g. weather: rain/wind at multiple levels, heat, humidity, dryness, and so on).
- For each variation, lock the logic split by player tier.
- Within each player tier, lock the logic split by prop line.
- Within each prop line, lock the logic split by variation/direction.
- Repeat this same full structure for every other raw factor (lineups, umpire, bullpen, schedule spot, park, catcher context, market/odds-adjacent factors, etc.).
This produces an extensive, pre-designed base framework covering every real scenario. At actual run time, scoped to the current slate/board, Enrichment's real-time job is reduced to a classification/lookup: identify which pre-locked logic applies to each leg for each relevant factor × player-tier × prop-line × variation/direction combination — the heavy design work is already done, run-time is just qualification, not fresh computation. This mirrors the classification_v6 pattern (heavy tiering/calibration work done once, applied cheaply per-run).

### Final Hit Probability calculation
Uses the enrichment phase's classified logic/metrics to calculate each leg's real hit probability. Confidence is recalculated here too, using the same logic/reliability-weighting approach used in the baseline (classification_v6/baseline_v6), not reinvented.

### Final Score
Each leg gets a score from confidence vs. final hit-probability percentage (the specific scoring formula/curve is not yet locked — needs its own research/lock when this phase is reached).

### Final Board — explicit selection rules (locked, verbatim)
Base inclusion rule: only legs with final hit probability over 65% are eligible at all.
On top of the 65% floor, quotas that qualify regardless of relative rank within that pool (a leg can satisfy multiple quotas simultaneously — no exclusivity):
- Best 5 legs of each individual prop line
- Best 15 legs of unders
- Best 20 legs of overs
- Best 20 goblins
- Best 5 demons
- Best 10 regular (standard) lines
- Best 20 legs of each app/source
Quotas are soft ceilings, not hard requirements — if fewer legs than a quota's number exist (e.g. fewer than 10 regular lines total), include all that exist, don't pad or lower the 65% floor to fill a quota.

### Resource-efficiency requirements, locked
- Every phase from here on must consume Daily Context's and Market Context's real output (both game-level and player-level) — no separate/duplicate mining.
- Started/live games' legs are never calculated by this pipeline (matches the existing `pickable_safe`/`calendar_matched` exclusion pattern already used everywhere upstream).

### New certifier + new full run required for this entire sequence
Same bar as Daily Context and Market Context's certifiers: covers orchestrator wiring, Control Room (worker + static HTML) parity, all miner/calculator workers in this sequence, and everything behind it. Build this the same way the Market Certifier/Market Full Run was just built and hardened this session (transient-retry, priority-tiebreak-safe, real per-layer tally) — do not regress on any of the reliability lessons just learned there.

### Process requirements, explicit, non-negotiable
- Break the work into multiple micro-phases (not one big build).
- Extensive, reliable-source online research required per phase — reference real, established systems/methodologies, not invented logic. Enrichment specifically needs an extremely exhaustive research pass to lock the right number of profiles/tiers before any code is written.
- No flattening, no summarizing anywhere in this pipeline — full custom, microscopic-level granularity, and the design must remain customizable at that same granularity going forward.
- After the research/lock phases (a few hours of work), Board, Daily Context, and Market Context must all be refreshed so the rest of the build works against genuinely current data.
- Only move to the next phase/worker once the current one is fully correct with no further improvement identified — do not batch multiple unverified phases together (same discipline as Daily Context Phase 4's one-worker-at-a-time rule).
- Do not deviate from this locked structure. Any deviation or workaround requires his explicit approval before proceeding — do not just make a reasonable-seeming substitution and proceed.

### Sequence for this build, as instructed
1. Document this plan in full here (this section — done).
2. Recognition phase: map what already exists for Prop Factor Miner and Matrix Builder (real code, not filename/assumption — same "deployed slot vs. logical name" trap already flagged below in the "PROP FACTOR MINER / MATRIX - REAL NAMING CONFUSION" section; verify current behavior against the CURRENT system, not what it may have been built against originally).
3. Deep research + lock scope for each phase individually, Enrichment above all needing the most exhaustive pass.
4. Refresh Board/Daily Context/Market Context once research/lock is done, before building.
5. Build phase by phase, one at a time, verified with real data before moving on.

### PROGRESS LOG (update as work actually happens; nothing marked done until verified against real data)
- Documentation: COMPLETE (this section).
- Recognition (Prop Factor Miner + Matrix Builder real code): COMPLETE, verified.
  - Both are pure aggregation/eligibility workers, no factor-influence or scoring logic in either - confirms Enrichment is genuinely new work, not a revision of these two.
  - Prop Factor Miner: builds one packet per safe prepared leg combining game/player/base-metrics/daily-context/market-context/expansion-baseline-HP, tags real/derived/missing/warning.
  - Matrix Builder: reads Prop Factor coverage + market/daily readiness, builds one row per safe prepared leg with a status (ready/warnings/partial/blocked/deferred) plus side/variation context, includes empty `scoring_placeholders` clearly reserved for a later phase.
  - Both kept, both needed real adjustment (below) rather than a rebuild.
- Adjustment for expanded prop lines/board (Rodolfo's explicit instruction) - COMPLETE, verified with real data:
  - **Real bug found and fixed**: both workers hardcoded two independently-maintained HITTER_PROPS/PITCHER_PROPS Sets that had already silently drifted from each other and from the real taxonomy - Matrix Builder was missing `runs_allowed` (a real, currently-supported canonical prop) entirely, which would have blocked every such leg there while the Miner handled it fine. Root-caused and fixed properly: both workers now read the single real source of truth, `CONFIG_DB.config_prop_taxonomy` (21 real canonical prop keys), cached once per invocation, instead of hand-maintained lists that can silently diverge again.
  - **Real bug found and fixed**: Matrix Builder's side/variation logic (goblin/demon/standard/two-sided classification) only recognized PrizePicks and Sleeper by hardcoded source name - Underdog fell through to `side_availability_unclear` (blocked) for every single leg. Root cause was two-fold: (1) the real source_key is `parlay_underdog`, not `underdog` - fixed the name normalization; (2) the side-mode decision was a source-name chain with no Underdog branch at all - replaced with a generalized, price-based rule (real over/under price presence determines two-sided/one-sided) that correctly handles Underdog (confirmed via live data: real top-level `over_price`/`under_price` both present) and any future source without needing its name hardcoded, while keeping PrizePicks' goblin/demon/standard special-cased since those are boosted one-directional projections, not real two-sided priced markets.
  - **Verified live**: Prop Factor Miner (hitter mode) ran 3 consecutive chunks cleanly post-fix, `expected_factor_rows` correctly reflecting the real combined PrizePicks (5,131 rows) + Underdog (1,181 rows) taxonomy-eligible universe, no crashes, sensible warning/missing counts throughout. Matrix Builder ran its first 5 chunks (375 rows) cleanly post-fix with sensible status distribution (partial_context/blocked/source_missing, no crashes) before a real background lock-hold delayed further progress (matches the documented, self-healing "GLOBAL_ORCHESTRATOR lock busy" pattern from earlier this session, not a new bug) - chunk processing order hadn't yet reached an Underdog-source row at last check; the code fix itself is confirmed correct by direct inspection (real over_price/under_price present on every sampled Underdog row → generalized rule assigns two_sided) even though live confirmation of an actual Underdog matrix row was still pending completion of the chunked run at last check.
- Research/lock (per phase): NOT STARTED.
- Board/Daily Context/Market Context refresh: NOT STARTED (deferred until after research/lock, per explicit sequencing).
- Build: NOT STARTED.


## UPDATE 2026-07-13 — PRE-SCORING INFRASTRUCTURE PHASE: LOCKED PLAN BEFORE IMPLEMENTATION

Rodolfo has paused the Final Scoring Sequence build to do foundational infrastructure/data work first, explicitly acknowledging this delays Scoring but wants it "100% ready for whatever comes, including next year's season." Full scope below, locked before implementation, same discipline as every other phase in this document.

### Real database inventory check (done before allocating anything)
`check_bindings` revealed a binding not yet used anywhere in this session's recognition work: `CONTEXT_DB` (`alphadog-v2-context-db`, real id `ca44e9a7-0624-4ac7-857e-5e2af70c6b8f`). Checked directly, not assumed: contains dormant, empty `context_packets`/`context_factor_scores` tables (0 rows each - almost certainly an abandoned earlier ChatGPT-era precursor to the Prop Factor Miner/Matrix Builder that now live in SCORE_DB) alongside **live, real data** in `expansion_first_inning_*` tables (1,428 + 2,856 real rows - the RFI/NRFI "expansion" system, confirmed still-live per earlier handoff notes on RFI/NRFI). Total DB size ~22MB - by far the lowest-usage real database in the account. Do NOT touch/rename the live `expansion_first_inning_*` tables; safe to add new tables here for backfill data.

### Database allocation decision, locked
1. **CONTEXT_DB** - repurposed to hold the historical backfill / expanded static reference data (multi-season game logs, weather, market, umpire, pitcher-arsenal, defensive-quality, catcher-framing history). Matches Rodolfo's own framing: "the previous season and historic data is static data."
2. **New, dedicated database for the Scoring system** (Enrichment, Final HP, Final Score, Final Board) - genuinely separate from everything else, provisioned fresh, explicitly for future growth/scale isolation per Rodolfo's request. Real mechanism found (not assumed): `cloudflare_d1_bindings.json` is the single source of truth consumed by `generate_wrangler_configs.py`, which binds every listed D1 database to every worker uniformly (no per-worker filtering) - adding one new entry here makes it available everywhere automatically. `CLOUDFLARE_API_TOKEN`/`CLOUDFLARE_ACCOUNT_ID` are already present as GitHub Actions secrets and Wrangler is already installed in the deploy workflow, so provisioning happens via a real, idempotent Wrangler CLI step added to the existing deploy workflow (checks if the binding already exists in the committed JSON first; only creates + commits a new one if missing) - no manual Cloudflare dashboard step needed from Rodolfo.

### Full phase sequence, locked (do not reorder without a real reason, log it here if you do)

**Phase 1 - Infrastructure**
1a. Add the new Scoring database via the idempotent Wrangler-CLI deploy-workflow step described above; confirm the new binding (working name: `SCORING_DB`) is live via `check_bindings`.
1b. No new provisioning needed for CONTEXT_DB reuse (already exists, already bound) - just build new tables into it in Phase 3.

**Phase 2 - Recognition: Static Full Run's real current structure**
Read the real, current static-phase workers (teams, stadiums, park factors, players, prop taxonomy, static certifier, static-full-run orchestration/stage wiring) before designing the differential redesign - same "understand before changing" discipline as every other phase.

**Phase 3 - Historical backfill (direct, no orchestrator - fastest correct path per Rodolfo's explicit instruction)**
Done as one-off scripts/direct SQL, not scheduled/orchestrated jobs, since these never need to run again once complete:
- 3a. Real historical actual outcomes (hitter/pitcher game logs) for prior season(s) via MLB StatsAPI - the actual training labels; contextual backfill is useless without this.
- 3b. Real historical weather via Open-Meteo `/v1/archive` (free, no key, ERA5 back to 1940) for every backfilled historical game date/venue.
- 3c. Real historical game-level market context via ParlayAPI's historical/closing-lines endpoint (paid per-call, real data) - game-level only (implied team runs/totals); historical prop lines are not needed for training per the rate-model design already locked.
- 3d. Real historical umpire assignments via Gemini + Google Search grounding (the one legitimate case for LLM-assisted lookup - retrieving/citing real public record, not estimating) - bounded, one-time backfill, not a recurring operational dependency.
- 3e. Real historical pitcher arsenal/stuff, defensive quality (Outs Above Average), and catcher framing/pop-time via Baseball Savant's year-parameterized leaderboards (same trusted, already-proven, free, non-paywalled source as this session's existing Catcher Context work) - single mechanism covers all three plus extends catcher framing's own history.
- Real fatigue-factor backfill (bullpen/schedule-spot) needs no external call at all - recomputed directly from 3a's own historical box-score data using the existing computation logic, just pointed at historical dates instead of "today."
- Real question to check before executing at volume, not assumed: Baseball Savant's practical rate-limit/volume tolerance for a multi-season, multi-leaderboard backfill.

**Phase 4 - New ongoing mining layers for the genuinely new factors**
Rodolfo's instruction: adjust mining to where it actually belongs; only build a new Daily Context layer if the factor is genuinely daily-context-shaped. Real call, made explicitly: pitcher arsenal, defensive quality (OAA), and catcher framing are all season-level, slow-changing aggregates (matches how Catcher Context's framing/pop-time already works - refreshed only when >20h stale) - these belong as **static/reference data**, not a new per-day Daily Context mining layer. Plan: extend the existing reference-refresh pattern (same as `REF_DB.ref_catcher_framing_poptime`) with two new tables (pitcher arsenal, defensive quality), refreshed on the same slow cadence; extend the existing per-game join pattern (same as `DAILY_DB.daily_catcher_context_current`) to surface today's real starters/lineup against these new reference tables for daily consumption. Full orchestrator/certifier wiring only where a genuine daily join step is needed (the per-game join), not for the slow-changing reference refresh itself.

**Phase 5 - Static Full Run: differential redesign + real weekly schedule**
Rodolfo's explicit, standalone ask, been "postponed" until now: the Static Full Run currently re-mines its whole universe every run; needs to become differential (detect and mine only what's actually changed - roster moves, new/retired players, updated reference values - not a full re-mine), and needs a real weekly schedule at 2:00 AM Pacific. Real, already-existing infrastructure found: `ORCHESTRATOR_CRONS` in `generate_wrangler_configs.py` already includes a weekly Monday cron (`"0 3 * * 1"`, UTC) - needs verification of what it currently triggers and correction to the real Pacific-2am-equivalent UTC time (accounting for DST) if it's meant to be this job, or a new cron slot added if not. Full recognition of the current static-full-run code required before designing the differential logic - not assumed from the phase name alone.

**Phase 6 - Daily Delta Full Run: roster-adjustment backfill check**
Rodolfo's explicit ask: verify whether the existing daily delta full run (classification_v6/baseline_v6 pipeline) correctly picks up roster changes (trades, call-ups, etc.) for players already in its rolling data, or whether stale team/roster assignments could persist. Real investigation required before assuming either way; patch only if a real gap is found.

**Phase 7 - Testing discipline, explicit and locked**
- Anything from Phase 3 (backfill) that will never run again on a schedule: done directly (SQL/scripts), no orchestrator involvement, fastest correct path - per Rodolfo's explicit instruction.
- Anything that will have a recurring scheduled run (the fixed Static Full Run above all): needs real orchestrator-based testing before being considered done - same rigor as every other phase/full-run built this session.

**Phase 8 - Resume the Final Scoring Sequence** exactly where it was paused, once Phases 1-7 are real and verified.

### PROGRESS LOG (update as work actually happens; nothing marked done until verified against real data)
- Plan documentation: COMPLETE (this section).
- Phase 1 (database infrastructure): COMPLETE, verified.
  - `SCORING_DB` (real, dedicated Cloudflare D1 database `alphadog-v2-scoring-db`, id `584fd46e-cbe0-4005-9c1f-20a912c77dc5`) provisioned via a new, idempotent Wrangler-CLI step added to the deploy workflow (`ensure_scoring_db.py`) - took 3 real debugging iterations, each diagnosed via a committed debug log file (since GitHub Actions run logs aren't directly viewable from this session) rather than guessing: (1) this Wrangler version doesn't support `--json` on `d1 create` at all: "Unknown argument: json"; (2) first regex fix assumed TOML-style `key = "value"` output but this version's real output is JSON-style `"key": "value"`; (3) fixed regex handles both, confirmed the database was already really created by attempt 1 and got registered via the "already exists" fallback path without creating a duplicate. Verified real: `run_sql` against `SCORING_DB` now works (after also fixing `alphadog-v2-admin-sql.js`'s own `REQUIRED_DB_BINDINGS` enum, a separate real gap that would have silently blocked querying it).
  - `CONTEXT_DB` confirmed low-usage (~22MB, dormant legacy tables alongside live RFI/NRFI data left untouched) and reserved for backfill data.
- Phase 2 (Static Full Run recognition): COMPLETE, verified against real code, not the phase name.
  - Real chain confirmed: 6 stages (Teams, Stadiums, Park Factors, Players, Prop Taxonomy, Certifier). The other 4 "static-*" workers in the manifest (`static-rosters`, `static-player-identity`, `static-player-aliases`, `static-team-context`) are confirmed genuine, never-built dummy stubs (`VERSION: "alphadog-v2-dummy-workers-v0.1"`, zero real logic) - not a missing-wiring gap, `static-players.js` already comprehensively covers roster/player-identity/alias data in one consolidated worker.
  - Read `static-players.js` and `static-teams.js` fully: both do a full re-fetch + full re-write every run, no change-detection anywhere - confirmed this is the real, current behavior across the static phase, not assumed from the phase name.
- Differential redesign: COMPLETE across the entire real static chain, all verified live.
  - **static-players.js**: real differential built and verified live - `real_differential_rows_changed_this_run: 3` out of 1,341 total active players (~99.8% write reduction), correctness preserved (1,341 players, 30/30 teams, 0 duplicates/missing).
  - **static-teams.js**: verified live - `teams_written: 0, teams_unchanged_skipped: 30` on a same-day repeat run.
  - **static-stadiums.js**: real differential built (this worker previously did an unconditional deactivate-all-then-reactivate-all cycle every run - two full writes per row regardless of change - now loads a snapshot first and only pays the full-rewrite cost for genuinely new/changed stadiums). Verified live - `stadiums_written: 0, stadiums_unchanged_skipped: 30`, correctness preserved (30 active, 270 aliases).
  - **static-park-factors.js**: real differential built (same pattern; park factor numbers can legitimately drift a little as more games are played, so some real writes are expected here over time, unlike team/stadium identity). Verified live - `park_factors_changed_this_run: 0, park_factors_unchanged_skipped: 30`, 0 missing factors, validation passed clean.
  - **static-prop-taxonomy.js**: checked, already inherently differential by design (`if (!before) INSERT` - only ever adds genuinely new prop keys, never rewrites existing ones) - no fix needed.
  - **static-certifier**: read-only validation only, no writes to make differential.
  - **Real, separate orchestrator bug found and fixed while testing** (not caused by the differential changes themselves): `processStaticTeamsJob`, `processStaticStadiumsJob`, and `processStaticParkFactorsJob` had each been accidentally left with a full copy of the Prop Factor Miner's chunked-cooldown dispatch logic (`isPitcherMode`/`selectedMode`/`rowInput`/`prop_factor_batches` references, none of which apply to these simple, single-pass static workers) pasted in ahead of the real dispatch, and `partialContinue` was referenced later in each function but never actually defined - guaranteeing a crash ("partialContinue is not defined") on every real run. Confirmed via a live test that crashed on the very first attempt for static-teams. Fixed minimally and safely in all three (added `const partialContinue = false;`, the correct value for a non-chunked worker, rather than risk a large, error-prone removal of the dead surrounding block) - **verified live for all three**: static-teams, static-stadiums, and static-park-factors all now complete cleanly with real data.
- Real, separate diagnostic gap found and fixed (from a direct request to cross-check unresolved board/market legs against the roster): the system only ever tracked an aggregate `unresolved_player_rows` *count* in `score_board_prep_batches`, never the actual player names - confirmed via real historical data this has recurred repeatedly (17, 32, 97, 29, 25+ unresolved rows across many past batches, as recently as the day before this session), but was undiagnosable after the fact since no name was ever retained. Fixed: added `score_board_unresolved_player_log` (real player name/source/prop logged at the moment of detection in `alphadog-v2-score-prep.js`, 30-day bounded retention) - the next real occurrence will be diagnosable instead of just a number. Not yet caught live in the act (current board is clean at time of writing) - this is now instrumented and waiting.
- Separate finding worth flagging, not yet acted on: discovered an entire pre-existing "v1/v2 Final Scoring System" (real tables: `score_enrichment_issues`, `hit_probability_issues`, `final_score_v1_issues`, `score_final_board_issues`, and `v2_*` equivalents) distinct from classification_v6/baseline_v6 and from the new Scoring build - needs recognition/reconciliation when Scoring resumes (Phase 8).
- Phase 5 (Static Full Run weekly cron correction): COMPLETE, mostly verified.
  - Real, existing "static-full-run" parent job_key mechanism found and used (matches the same pattern as board-full-run/daily-context-full-run - `isStaticFullRunJob`, `childPassedStaticFullRun`, `staticFullRunChildInput` already existed) - no need to rebuild chain-processing logic, only the scheduling trigger.
  - **Real bug found and fixed**: the previous weekly cron only ever enqueued `static-players` alone (never the full 6-stage chain), used a fragile fixed-UTC-string cron match (`"0 3 * * 1"`) that would drift with DST, and had a second real bug (referenced `rid()`/other players-specific logic in a spot that's now moot since the whole function was replaced).
  - Real, better mechanism used instead of raw cron math: `CONFIG_DB.config_scheduled_jobs`, an existing Pacific-timezone-aware (via `Intl.DateTimeFormat`, genuinely DST-safe) schedule table already used for board-full-run/incremental-morning-full-run/daily-full-run. Added real weekly support to it (new `day_of_week` column, additive) and a real Pacific weekday field to `pacificNowParts()`. Built `enqueueScheduledStaticFullRunIfDue`, modeled exactly on the proven `enqueueScheduledBoardFullRunIfDue`/`enqueueScheduledIncrementalMorningFullRunIfDue` pattern, checking day-of-week + local-time window before enqueueing the real `static-full-run` parent job. No cron-list changes needed - the existing `*/5 * * * *` cron already fires often enough for the internal Pacific-time window check.
  - Inserted the real, permanent schedule row: `job_key='static-full-run'`, `day_of_week='Mon'`, `local_time='02:00'`, `timezone='America/Los_Angeles'`, `schedule_type='weekly'`.
  - **Real, separate structural bug caught and fixed during this edit**: my first attempt at replacing the old broken function left its original body orphaned (dangling outside any function declaration) after the signature line got replaced - a genuine syntax error that broke the deploy. Caught immediately (deploy failure), diagnosed by re-reading the file structure directly, and fixed by removing the entire orphaned block cleanly.
  - **Verified**: deploy succeeded, `day_of_week` column confirmed present via direct schema check, a real post-deploy orchestrator cycle ran clean with no errors (`no_due_jobs`, correct given it isn't Monday 2am Pacific yet). Honest caveat: this confirms the code is syntactically sound and runs without crashing, but the manual-wake test path doesn't exercise Cloudflare's actual `scheduled()`-cron-triggered code path specifically - that gets its first real exercise on the next real cron tick, same as any other scheduled job in this system.
- Real, additive gap found and fixed (from an explicit request to audit for missed backfill factors) - two real fields existed in raw JSON already stored but were never flattened into queryable columns:
  - **`pitcher_game_logs.games_started`**: real starter/reliever designation (`raw_json.stat.gamesStarted`) existed for all 11,859 real rows but had no queryable column - `role` was always the generic literal "P". Added the column, backfilled all real rows (2,844 real starts / 9,015 relief appearances - a plausible, real ratio), and added a real SQLite trigger (`trg_pitcher_game_logs_games_started`) so it auto-populates on every future insert regardless of which of the 3 separate INSERT code paths in `alphadog-v2-base-pitcher-game-logs.js` writes the row - a trigger was used instead of patching all 3 fragile SQL statements individually, verified live with a real isolated test insert/delete.
  - **`hitter_game_logs.primary_position_played` + `played_catcher_flag`**: real per-game position data existed in raw_json but was unflattened. Real, additional finding while backfilling: the table has (at least) two different raw_json shapes from different ingestion/repair paths (`positionsPlayed[0]` vs `player.allPositions[0]`/`player.position` vs top-level `allPositions[0]`/`position`) - the first backfill attempt only checked one shape and left 14,880 of 29,921 rows NULL; caught by directly inspecting a real NULL-but-flagged-catcher row (Sandy León), not assumed. Fixed with a `COALESCE` across all real observed shapes - zero NULLs remain, real, sensible distribution across positions (~2,900-3,070 rows per position). Same real SQLite-trigger approach applied (`trg_hitter_game_logs_position`) for future inserts, verified live.
  - This directly unblocks accurate historical Catcher Context backfill (`played_catcher_flag`) and gives a real, queryable starter/reliever signal for historical pitcher features - both needed for the Phase 3 backfill and for Enrichment's future feature set, at zero external-call cost since the data was already present.
- Phase 4 (new factor mining layers - pitcher arsenal, defensive quality): NOT STARTED.
- Phase 6 (Daily Delta roster-adjustment check): COMPLETE, verified - **no real gap found, no fix needed**.
  - Real investigation, not assumed either way: checked whether classification_v6/baseline_v6 or the daily delta pipeline could carry a stale team reference for a traded/newly-active player.
  - `classification_v6_current` schema checked directly - it has no `team_id` column at all; it's purely player-centric (player_id + prop + tier/z-score), so there's no stale-team-reference risk within that table by construction.
  - `hitter_game_logs`/`pitcher_game_logs` confirmed to carry a real, per-game `team_id` - verified against real data: 5 real players found with genuinely 2 distinct team_id values in their 2026 game logs (real mid-season trades), and cross-checked one (Randal Grichuk, player_id 545341) against `ref_players.current_team_id` - correctly matched his real current team (145/CWS).
  - The real mechanism that determines which players get recalculated each daily delta run (`selectAffectedHitterMetricPlayers`/pitcher equivalent) is driven by `SELECT DISTINCT player_id FROM hitter_game_logs WHERE season=? AND game_date > baseline...` (and the identical pattern on the pitcher side) - genuinely team-agnostic: any player with a real new game log row gets included, regardless of which team they played for, and this naturally covers brand-new call-ups too (no pre-existing-player filter). No stale roster/team snapshot is used anywhere in this selection.
  - **Conclusion delivered honestly**: the daily delta pipeline does not have a roster-staleness gap. It's correctly driven by real game activity rather than any cached team/roster assumption. No patch was needed or made - a real, complete "checked and it's already correct" outcome, not a manufactured fix.
- Phase 7 (orchestrator-based testing for scheduled items): PARTIALLY DONE as part of the differential work above (static-teams live-tested through the orchestrator; static-players live-tested through the orchestrator in a prior update).
- Phase 8 (resume Final Scoring Sequence): NOT STARTED.

`alphadog-v2-control-room.js`'s own debug config reveals that the "FACTORS" and "MATRIX" buttons Rodolfo will eventually use for the next phase are **deployed to repurposed worker slots with different names than their logical function**: `prop_factor_deployed_slot: "alphadog-v2-phase2b-recent-form"` (logical worker: `alphadog-v2-prop-factor-miner`), and `matrix_deployed_slot: "alphadog-v2-phase2b-certifier"` (logical worker: `alphadog-v2-prop-matrix-builder`). This is the same kind of "deployed slot vs. logical name" mismatch already flagged for Daily Context's Umpire worker (`alphadog-v2-daily-usage-pulse.js` = `daily-umpire-context`) - a recurring pattern in this codebase. When Rodolfo's stated next phase (Prop Factor Miner, which he's explicitly said he doesn't understand and was built by a different assistant) is reached, read `alphadog-v2-phase2b-recent-form.js` and `alphadog-v2-phase2b-certifier.js` by their real deployed content, not by filename assumption.


- `github_patch_file` for edits to large files (`alphadog-v2-orchestrator.js` is 1.29MB+, too large for `github_get_file`'s fallback — `github_patch_file` still works via the Git Data API path automatically; you can't pull the whole file back to syntax-check locally, so rely on the GitHub Actions deploy succeeding plus targeted `github_grep_file` checks of exactly what you changed).
- **Test small, isolated changes with a real syntax check before trusting a deploy** — caught my own off-by-one paren error this way (built the exact pattern in a scratch file, ran `node --check`) before it could cost more real debugging time.
- Any edit to `generate_wrangler_configs.py` triggers a full ~15-minute redeploy of all workers (it's in `GLOBAL_REDEPLOY_FILES`). Any other single-file edit is a fast ~1 minute single-worker redeploy.
- **Test the exact SQL/query logic directly against live data before writing the corresponding code fix** — this is what made the orchestrator hot-lane fix reliable on the first real rewrite rather than another guess-and-check cycle.
- Repeated manual `orchestrator_tick` wake calls stacked on top of natural cron cadence caused a real duplicate-child-creation race this session — prefer a single enqueue then patient, spaced-out checking over repeated manual wakes when testing a chain end to end.
- **Do not accept "it's an external/unfixable condition" at face value** — verify directly. This session that meant web-searching and fetching the third-party provider's own documentation and live public status endpoints, then building a real, isolated diagnostic test (a raw `INSERT` into the job queue with a parameter override) using the actual production credentials, bypassing our own pipeline entirely, to get a definitive, evidence-based answer rather than an assumption.
- **Do not just check that a fix deployed — verify the fix actually changed the observed behavior**, ideally by re-running the exact scenario that exposed the bug.
- When something looks wrong in a log Rodolfo pastes, do not assert what's "supposed to" happen from memory — pull the actual current data/queue state and check.
- Update this document proactively, not in a big batch, when a session covers substantial new ground.
- **When editing a large, risk-prone INSERT statement (many interspersed literals and placeholders), prefer a safe, append-only edit** (add new columns/placeholders at the very end of the column list and VALUES clause, leaving the entire existing middle untouched) over restructuring the middle - this avoids needing to recount dozens of existing placeholders and was what caught/avoided a repeat of an earlier placeholder-count mismatch bug.
- **A tool call occasionally errors out transiently** (`"Error occurred during tool execution"` with no other detail) even when the underlying dispatch actually succeeded or is still pending - check the real state via a direct SQL query before assuming the action failed and needs to be redone.
- **`lock_busy` responses from the orchestrator are correct, safe behavior, not a problem** - it means a real background cycle (cron or a prior pump's self-continuation) already holds the `GLOBAL_ORCHESTRATOR` lock (up to 5 minutes per acquisition). Repeatedly re-pumping into a busy lock wastes calls; wait a meaningful amount of time and check status via lightweight SQL instead.
- **A job stuck in `running` status for several minutes with no `updated_at` progress is a real stall worth investigating, but the system's own stale-running-job recovery will typically detect and retry it automatically** (observed directly working for Daily Context Full Run's player-availability stage this session) - the original stuck attempt gets marked `failed` cleanly, a fresh attempt spawns under a new request ID, and the parent chain proceeds once it succeeds. Give this a real chance to self-heal (a minute or two past the point it looked stuck) before assuming something needs manual intervention.

## UPDATE 2026-07-12 (LATER SAME DAY) — DAILY CONTEXT FULL-RUN RELIABILITY: REAL ROOT CAUSE FOUND, FIXED, RE-VERIFIED MULTIPLE TIMES. UMPIRE TIER REORDER + REFMETRICS SHIPPED. NEW DIAGNOSTIC INFRASTRUCTURE BUILT.
Two sessions of real work since the "FULL RUN LIVE VERIFICATION - IN PROGRESS" section above was written. That section's "not yet confirmed complete" tail is now resolved — full detail below, same rigor bar (nothing here claimed without direct evidence).

### Umpire tier reorder + RefMetrics integration — DONE, verified
Umpire Context's real-source tier order changed to: (1) MLB StatsAPI official, (2) **RefMetrics direct authenticated fetch (new)**, (3) Gemini+Google Search grounding (last resort, capped at 4 calls/run, 6s timeout — was 10 calls/12s, which risked blowing the aggregate dispatch budget on a large slate), (4) internal crew-rotation history fallback (Phase 5's original logic, now the final fallback not the first).
- RefMetrics requires a login (Flask-style CSRF: GET for csrf_token+cookie → POST credentials → capture authenticated session cookie, 20-day cache) — credentials stored in `CONFIG_DB.config_external_credentials` (key `refmetrics_login`), session cached in new `CONFIG_DB.config_external_sessions`. Real, verified finding: RefMetrics' umpire name field is paywalled when unauthenticated (shows a lock icon) but fully accessible once logged in with the free account.
- **Verified with real live dispatch**: 14/14 games covered via `derived_from_refmetrics_direct`, 0 Gemini calls needed, real named umpires returned (not placeholders).
- All 4 RefMetrics fetch calls now have an 8s `AbortSignal` timeout (`refMetricsFetch()` wrapper) — this whole worker had a per-run cache variable (`refMetricsRunCache`) added too, explicitly reset at the top of every invocation so a warm isolate reuse can't serve stale data across runs.

### Starters worker — real, separate timeout/concurrency bug found and fixed
`alphadog-v2-daily-probable-pitchers.js`'s `fetchJson()` had **zero timeout** on its external calls (same bug class the Board phase had and fixed last session — this worker had been missed). Fixed with an 8s per-call `AbortSignal` timeout, and converted `fetchActualStarterMap`'s live-feed lookups from fully sequential to bounded concurrency (6-at-a-time), matching the established Board pattern. Note: this worker runs in a deliberate "timeout-safe default" mode (`live_feed_actuals_enabled: false` — schedule/probable-pitcher hydrate only, no live-boxscore actual-starter verification) specifically to avoid the risky sequential live-feed loop during full-run dispatch; this is intentional, not a regression.

### THE REAL STORY — WHY FULL RUNS WERE INTERMITTENTLY FREEZING FOR ~5 MINUTES AT RANDOM STAGES, ROOT-CAUSED WITH ACTUAL EVIDENCE
This was the main body of work this session, and it went through several wrong-then-right turns — documented honestly so the next session doesn't repeat them.

**Symptom, reproduced repeatedly, real not anecdotal**: the `daily-context-full-run` chain would periodically freeze at whatever stage happened to be dispatching next, for close to exactly 5 minutes, then self-recover on its own (via the `GLOBAL_ORCHESTRATOR` lock's own 5-minute `expires_at`) with no data loss — the stage would end up re-attempted and complete correctly. Never data-corrupting, always time-costing.

**First (wrong) hypothesis, corrected before it wasted more effort**: initially looked like the orchestrator's recursive self-continuation (`ctx.waitUntil()` chain, `pump()` calling itself) was starving itself out via job-priority ordering — real starvation bug found and fixed anyway (see below), but it was NOT the actual root cause of the freezes.

**Real diagnostic tooling built to stop guessing**:
1. First built a Cloudflare GraphQL Analytics (`workersInvocationsAdaptive`) query job (`worker_invocation_logs`, added to `alphadog-v2-admin-sql.js`'s `toolRunJob`). Required wiring `CLOUDFLARE_API_TOKEN`/`CLOUDFLARE_ACCOUNT_ID` (already present as GitHub Actions secrets for wrangler auth, but NOT previously pushed to any Worker's own runtime — added to `github_write_worker_secrets_file.py`'s existing dict, which was already present but had never actually been deployed to admin-sql until a fresh deploy ran). **Found a real, important limitation of this approach and said so rather than trusting a false-looking result**: this dataset does not surface service-binding-triggered invocations at all — proved this by checking a worker (`alphadog-v2-daily-weather`) known for certain to have completed successfully, which also showed zero rows. Correctly did not use this as evidence of anything past that point.
2. **Built a real Cloudflare Tail Worker instead** (`alphadog-v2-tail-logger.js`, new, added to `worker_manifest.json` and wired as a `tail_consumers` entry on the orchestrator's generated wrangler config in `generate_wrangler_configs.py`) — this is the correct instrument: it receives the true outcome (`ok`/`exceededCpu`/`canceled`), real cpu/wall time, and any exceptions for every invocation of whatever it's attached to, including ones triggered via service binding, which the Analytics API cannot see. Writes to a new `CONTROL_DB.tail_worker_events` table; queryable via `/query?script_name=...&minutes_back=...` on the tail-logger's own URL.
   - Real deploy-ordering bug hit and fixed immediately: the orchestrator's generated config includes a full service-binding mesh to every other worker in `worker_manifest.json` (including the new tail-logger), and workers deploy in manifest list order — tail-logger was originally appended to the very END of the list, after the orchestrator, so the orchestrator's own deploy failed validating a service binding to a worker that didn't exist yet (`Service binding 'TAIL_LOGGER_WORKER' ... was not found`). Fixed by moving tail-logger's manifest entry to just before the orchestrator.

**The real finding, captured directly, not inferred**: a genuine runtime log, captured via the Tail Worker: `"waitUntil() tasks did not complete within the allowed time after invocation end and have been cancelled."` This is Cloudflare's own runtime, not application code, confirming: the platform enforces a real per-invocation wall-clock ceiling (observed right around 30 seconds in the captured samples) on background (`waitUntil`) work — silently, with no JS-catchable exception, no matter what timeout logic exists in application code, because the runtime tears down the whole execution, not just one promise.

**Two real fixes followed from this, both verified against re-triggered live behavior, not just deploy success**:
1. **Chain-age ceiling added to the recursive self-continuation loop** (`pump()` in `alphadog-v2-orchestrator.js`). Previously `depth`/`maxChains`/`deadlineMs` bounded each individual hop's own time and recursion count, but nothing tracked the *cumulative* wall-clock age of the whole chain across hops — several individually-safe hops could still add up past Cloudflare's real ceiling. Added `chainStartedAt` (threaded through every recursive call, defaulting to the first call's own `Date.now()`) and a `CHAIN_TOTAL_WALL_CEILING_MS = 30000` gate on `shouldSelfContinue` — once the chain is 30s old, it stops scheduling further recursive work (falls back to the next cron tick) instead of gambling on staying under Cloudflare's ceiling. **Verified**: post-fix, captured a real invocation running a full 60+ seconds with a clean `outcome: "ok"` and no cancellation warning — proof the *mechanism* changed (lets in-flight work finish naturally instead of getting killed mid-flight), not just a hopeful guess.
2. **The deeper, more direct fix — `DAILY_CONTEXT_EXACT_WORKER_TIMEOUT_MS` reduced from 45000 to 20000.** Real root cause: the orchestrator synchronously awaits each child worker's full response (needed to build the stage report), using this constant as its own `AbortController` timeout. At 45000ms, any child taking longer than Cloudflare's real ~30s platform ceiling (which happens routinely — certifier/lineups/schedule-spot all normally take 15-30s) meant the orchestrator's OWN invocation could get killed by the platform outright, mid-await, before its own 45s JS timer ever got a chance to fire and recover gracefully. Reducing to 20000ms means the orchestrator's own abort fires safely before the platform's real ceiling, turning a fatal, unrecoverable platform kill into an ordinary, catchable, already-well-handled `*_service_binding_timeout_after_20000ms` condition that the existing sidecar-recovery logic (which already existed for exactly this) handles cleanly. **Verified live, real captured example**: schedule-spot's dispatch hit exactly this new 20s timeout, recovered via `completed_recovered_from_team_schedule_sidecar_after_dispatch_timeout` (checked whether the child had actually finished writing its data in the background — it had — and certified the stage using the real data), all within the same continuous tick, no 5-minute stall.

**Real starvation bug, found and fixed separately (not the main root cause, but real and worth keeping fixed)**: the due-job dispatch query picks exactly one job per cycle, ordered by `priority ASC, created_at ASC` — but `daily-context-full-run`'s own re-queue-on-continuation logic (4 separate `UPDATE control_job_queue` call sites) never elevated its own priority, so it could in principle sit "due" indefinitely behind other background job types with a numerically lower priority value, cycle after cycle. Fixed: all 4 call sites now force `priority=1` (highest) on every self-continuation re-queue.

**Honest current state, not oversold**: the two real fixes above measurably reduce both the frequency and the blast-radius of these freezes — most full runs now complete with 0-1 stalls instead of getting stuck on nearly every stage transition, and when a stall does still happen, it typically now resolves via the existing fast sidecar-recovery path (seconds) rather than the full 5-minute lock-expiry path. **But a full run tested after all fixes above still hit the slow 5-minute-lock-expiry path once, on the very last stage (`daily_context_certifier`)** — captured via the Tail Worker showing **zero record at all** for that specific invocation (unlike the earlier, now-fixed cases, which at least logged a clean "ok" + cancellation-warning before returning). This is a meaningfully different, more severe failure signature: the whole invocation died outright before it could report anything, not just its background continuation. Real, honest interpretation given to Rodolfo: `AbortController.abort()` only cancels *this side's* "waiting for the response" logic — it cannot force-stop the *other* worker's execution if that worker's own logic is what's actually consuming shared platform resources at the moment Cloudflare's absolute per-invocation ceiling is hit. **Not fixed as of this update** — the one further lever identified and explained to Rodolfo (not yet built, he chose not to pursue it given the current trade-off is acceptable): restructure child dispatch to be fully fire-and-poll (enqueue and return immediately; a later tick checks the database for completion) rather than fire-and-synchronously-await, which would remove the orchestrator's exposure to this failure mode entirely at the cost of a real, more invasive restructuring of how each stage's certification data gets collected into the parent's report.

**Rodolfo's explicit call, recorded verbatim in intent**: occasional multi-minute delays with zero data loss/corruption (confirmed: every stall this session, including the last one, resolved into a correctly certified run with sane, real row counts — verified directly against `control_job_runs.certification_status` and the actual `_current` layer tables, not just the parent's own "completed" status) is an acceptable trade-off for him right now. Do not proactively push further into the fire-and-poll restructuring unless he asks — it's a real, available option, explained to him, deliberately not taken.

### Real-world data-freshness / mining-cadence guidance given (not a code task, a strategy question he asked directly)
Verified live, per-layer, which of the 7 Daily Context layers are genuinely mined (real) vs. derived on a normal single-morning run, by querying `data_source_level` directly against the actual current tables (not assumed from the certification text):
- **Real on a single morning run**: starters (official MLB probable-pitcher schedule), weather (real forecast), bullpen availability (internal verified history), player availability (official roster status), schedule spot (internal calendar/travel — authoritative by design, not a fallback).
- **Derived on a single morning run, by design, not a bug**: umpire crews (MLB doesn't publish these until day-of, sometimes hours before), lineups (MLB doesn't confirm these until ~3-4 hours before first pitch).
- Advised: a single morning run is not enough to capture real (non-derived) lineups/umpire data for most of the day's slate, since MLB simply hasn't published it yet at that hour, regardless of how well the pipeline runs. Recommended cadence (Eastern Time, MLB's own reference clock) for real-world best edge: ~7:00 AM ET (baseline), ~11:00 AM ET (earliest day-game lineups), ~1:30 PM ET (afternoon + first evening-lineup wave), **~4:00-4:30 PM ET (the important one — catches the bulk of 7:05/7:10/7:40 PM starts, the most common MLB start slot)**, ~6:00 PM ET, optionally ~7:30 PM ET for west-coast late starts. Minimum practical cadence for "good": 3 runs (~7 AM / ~1 PM / ~4 PM ET). This is a manual/business-timing recommendation, not a system cron change — nothing was scheduled or automated for this; it's Rodolfo's call on how/when to trigger runs (currently a manual "Full Run" button press per the existing Control Room UI).


