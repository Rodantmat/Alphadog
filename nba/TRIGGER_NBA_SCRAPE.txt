This file's only purpose is to trigger .github/workflows/nba-scrape.yml on demand.

Any commit that changes this file's content fires the "NBA Static Data Scraper" workflow via
its `push` path filter - this works with plain github_put_file/github_patch_file, which every
NBA chat has had since day one, so no special tool or connector reconnect is ever needed to
run this scraper on demand again.

last_triggered_utc: 2026-09-03T06:00:00Z
trigger_reason: fixed a real bug - player position was never actually populated despite a column existing for it. leaguedashplayerbiostats does NOT have a position output field (verified via real schema, PlayerPosition is filter-only there) - added a second bulk call to playerindex, which does have a real POSITION field, confirmed via documented example response
